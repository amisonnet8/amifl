# AMIVM上で言語を実装するときのヒント(AmiFLの実装から)

> AmiFL(`github.com/amisonnet8/amifl`)の実装過程で得られた知見を、次にAMIVM上で別言語を実装するAI向けに申し送るためのメモ。
> AmiFL自体の言語仕様の話ではなく、「AMIVM-IRを生成するフロントエンドを書くときに踏む地雷・使える型」の話に絞る(Seed/Cascade/Weave各リポジトリの同名ファイルと同じ位置づけ)。
>
> 着手前に必ず`CLAUDE.md`の「AmiFL特有の設計課題」「過去に踏まれた地雷」節、および以下3つの先行実装の知見を読むこと——AmiFLはSeed/Cascadeと同じ静的型付けであり、特にこの2つは直接参考になる箇所が多い。
>
> - `ignored/seed_implementation_notes.md`(goto/VAR巻き上げ問題・スコープのフラット化・`CALL`の多義性・参照渡し/値渡しの基本方針)
> - `ignored/cascade_implementation_notes.md`(ポインタ・構造体・クロージャー・map・チャネル・ビット演算の実地検証結果。AMIVMの旧64命令をほぼ全て実証した実績があり、AmiFLが必要とする機能構成に最も近い)
> - `ignored/weave_implementation_notes.md`(動的型付け言語がAMIVMとどう相性が悪い/良いか。AmiFLは静的型なので大半は直接は関係しないが、`extern`機構の実装時は`gotype`/`gofunc`/`gomethod`の変遷が参考になる)

## 0. AmiFLが実証した命令

Step 13時点で、以下の命令だけで足りた。**実際に`amivm`→`go build`→実行まで通して動作確認済み。**

```
FUNC RET ENDFUNC
VAR SET
CALL
ADD SUB MUL DIV MOD
BAND BOR BXOR BCLEAR BNOT
SHL SHR
AND OR NOT
EQ NEQ LT LTE GT GTE
CONCAT
IF ELSE ENDIF
LOOP BREAK CONTINUE ENDLOOP
FNTYPE CLOS ENDCLOS
STTYPE FIELD ENDSTTYPE FSET FGET
SLTYPE SLMAKE SLICE ASET AGET
ARTYPE
MPTYPE MPMAKE MSET MGET MPKEYS
CHTYPE CHMAKE CHSEND CHRECV
SPAWN DEFER
METHVAL
```

**Step 13（`extern`機構）は`METHVAL`1命令だけを新たに実証した**——Goのレシーバ付きメソッドを「レシーバを普通の第1引数として受け取る関数」として`bind`する（`amifl-spec.md` 15.2節）際、AMIVMの`callname`オペランドがドット1個までしか許さない（`?pkg.Func`は書けても`?pkg.Type.Method`というGoのmethod expression構文は書けない）という制約に直面したが、`METHVAL local receiver <method`（`local := receiver.method`、事前の言語仕様調査時点では「AmiFLでは出番が無い見込み」と申し送っていた命令）で2命令構成にすれば正確に表現できることを実装前に発見できた——**AMIVM本体への機能要求は不要だった**。`ASSERT`は今回も出番が無かった（`Any`型境界の実装が実は`ASSERT`もreflectも必要としない、という発見については下記§1参照）。

Step 12（`Chan[T]`/`Stream[T]`/`File`I/O）は`CHTYPE`/`CHMAKE`/`CHSEND`/`CHRECV`/`SPAWN`/`DEFER`の6命令を新たに実証した。**`SEL`/`CASESEND`/`CASERECV`/`DEFAULT`/`ENDSEL`は結局一度も使わなかった**——事前の実装計画表はSELも「実証する命令（想定）」に含めていたが、実際に必要になった機能（`chan`/`send`/`recv`/`spawn`/`take`/`skip`/`collect`/`parallel`/`lines`）はどれも「1つのチャネルを順番に読み切る」か「複数goroutineの完了を待ってから閉じる」で足り、複数チャネルを同時に監視する非決定的選択が要る場面が一度も出なかった。Step 8がenum実装で新規命令ゼロだったのと同じく、「計画表の命令リストは想定であって確約ではない」という運用がここでも当てはまった。

Step 3で追加した前半（算術〜文字列連結）は`examples/operators.aml`が一括で実証している——自己検算式（演算子ごとの計算結果を積み上げ、期待値との差分をプロセスの終了コードへ埋め込む設計）になっており、`go build`→実行→終了コードの数値一致確認、という1本のテストで全命令をカバーしている。単項マイナス（`NEG`相当）に対応する命令はAMIVMに無く、代わりにリテラルへの符号直接付与（命令ゼロ）または`SUB 0 x`への展開（下記§1）で表現した。

Step 4で追加した`IF`/`ELSE`/`ENDIF`・`LOOP`/`BREAK`/`CONTINUE`/`ENDLOOP`は`examples/control_flow.aml`で実証している（同じく自己検算式で終了コード1本に集約する設計を踏襲）。**`ELIF`命令は意図的に一度も使っていない**——AmiFLの`elif`はパース時に`ELSE`+ネストした`IF`へ丸ごと展開する設計にした（CLAUDE.md「過去に踏まれた地雷」#2で事前に警告されていたとおりで、実装してみても本当にその通りだった）。

`main`のブリッジ（下記§1）で使う`CALL`のキャスト機能（`CALL %exitCode : ?int %code`、Seed/Cascade/Weave notes共通の「`CALL`はキャストにも使われる」パターン）も実証済み。`SET`はStep 2の`let`/代入で単純に「既存の`VAR`宣言済み変数へ値を書き込む」用途のみ使用（`%name value`という最も素直な形）だったが、**Step 7で複合代入（`x[i] = v`、`ASET`）の書き戻しにも使う場面が実際に出た**（下記§1参照）。

**`ARTYPE`はAmiFL側からamivm本体へ機能要求して追加してもらった命令**（CLAUDE.md「AMIVM本体への機能要求の判断基準」の基準4「表現力そのものを制限している」に該当すると判断——`VAR`の複合形`^[n]type1`が名前を持てず、多次元固定長配列を表現できなかったため）。Seed/Cascade/Weaveのいずれも固定長配列（Goのネイティブ`[N]T`）を必要としていなかったため、AmiFLが初めて踏んだ領域だった。

**Step 8（`enum`・`switch`拡張）は新規命令ゼロで実現できた**——`enum`はStep 6の`STTYPE`/`FIELD`/`ENDSTTYPE`/`FSET`/`FGET`（タグ付き共用体を「`Tag`フィールド+全バリアントのフィールドを1つの構造体へ統合」という設計で表現）、`switch`のパターンマッチはStep 4の`IF`/`ELSE`/`ENDIF`+`EQ`（`Tag`と各バリアントの位置番号を比較するネストしたif/elseチェーン）で完全に足りた。先行実装（Seed/Cascade/Weave）に前例が無い機能でも、既存命令の組み合わせの範囲内で表現できないか先に検討する価値がある、というCLAUDE.md「AMIVM本体への機能要求の判断基準」の判断順序（1. 既存命令の組み合わせで回避できないか→2. `amiflrt`で一元化→3. それでも無理ならAMIVM本体へ要求）がそのまま実証された形。

**Step 9（パイプ演算子`|>`）も新規命令ゼロだった**——`|>`はパーサーが`CallExpr`へ直接組み立てる純粋な構文糖衣（新しいASTノードすら無い）、`for...yield`はStep 7の`for`ループ（`LOOP`/`AGET`）とStep 7のList構築（`SLTYPE`/`SLMAKE`/`ASET`）の組み合わせで完結した。2ステップ連続で「新しい構文機能＝新しいAMIVM命令が要る」という直感に反する結果になった——中間言語が既に十分な表現力（構造体・添字アクセス・ループ）を持っていれば、その上に積む言語機能の多くは既存命令の**組み合わせ方**の問題に帰着する、という認識が強まった。

**Step 10（`Set[T]`・`Map[K,V]`）は3ステップぶりに新規命令カテゴリ（`MPTYPE`/`MPMAKE`/`MSET`/`MGET`/`MPKEYS`）が要った**——ただしこれはamivm本体への機能要求ではなく、既存のamivm仕様に元から用意されていた命令群を単に初めて使っただけ（Cascadeが実証済みの命令セットで、AmiFLでは初出）。Set/Mapという「連想配列的な構造」自体がList/Array/Tuple/enumの組み合わせでは表現できない（キーによるO(1)アクセス・任意のキー集合という性質はGoのmapが持つ固有の機構）ため、Step 8/9とは違い「既存命令の組み合わせでは足りない」正当な理由があった——CLAUDE.md「AMIVM本体への機能要求の判断基準」の1番目（コピー往復で回避できるか）にすら該当せず、素直にmap系命令が必要になった最初のケース。

**Step 11フェーズ11a（`Error`型・`?`演算子・`cast[T]`/`parse[T]`）も新規AMIVM命令ゼロだった**——`Error`はGoの`error`インターフェースへ直接マッピングするだけの型（`goTypeNames`への1エントリ追加）、`?`演算子は既存の`FGET`/`NEQ`/`IF`/`FSET`/`RET`の組み合わせ、`cast`/`parse`はGoの型変換・`strconv`パッケージをそのまま`CALL`で呼ぶだけで実現できた。Step 8/9と同じ「中間言語が既に十分な表現力を持っていれば、その上の言語機能は既存命令の組み合わせに帰着する」というパターンの3例目。

**Step 11フェーズ11b（13.4節データ操作28関数）も新規AMIVM命令ゼロだった**——ただし今回は「既存命令の組み合わせだけで足りた」というより「amivmの`CALL`が最初からGoジェネリクスの明示的型引数付き呼び出しに対応していた」ことが効いた（`CALL result : ?pkg.Fn<<^T,^U>> args...`という4.13節の拡張構文）。AmiFL自身はジェネリクスを持たない言語だが、実装言語であるGo側のジェネリクスを`amiflrt`の内部実装だけに閉じ込めて使うことで、28関数のうち14個を「型ごとに別のcodegenを書く」のではなく「1つのGo generic関数を型引数だけ変えて呼ぶ」で済ませられた——中間言語（AMIVM）がホスト言語（Go）の機能をそのまま素通しできる設計だと、フロントエンド言語自身が持たない機能（ジェネリクス）の恩恵を実装の裏側でだけ受けられる、という新しいパターン。

**Step 11フェーズ11c（13.5/13.6節Set/Map専用12関数）・11d（13.7節数値・13.9節`unwrap`/`okOr`、Step11最終フェーズ）も新規AMIVM命令ゼロだった**——11cはSetがStep10確定の`Map[T,Bool]`表現である恩恵（`toList`/`keys`が同一の`MPKEYS`呼び出しに帰着）、11dは`min`/`max`/`abs`/`clamp`を型非依存の`IF`/`ELSE`直接生成（amiflrtすら不要）、`unwrap`/`okOr`を`?`演算子と同じ`FGET`+`NEQ`イディオムの再利用で実現できた。Step 11全体（11a〜11d、約50関数）を通して新規AMIVM命令が一度も必要にならなかった——Step10（`MPTYPE`系）以来、命令追加が要らない実装が4フェーズ連続した計算になる。

## 1. 踏んだ地雷・確立したパターン

### `main`ブリッジの`os.Exit`境界で、AmiFLの`Int`（`^int64`）とGoの`int`が食い違う

`fn main() -> Int`の戻り値をamivmの`!main`（Goの`func main()`。引数・戻り値ともに無し）へ橋渡しするため、Seed/Cascade/Weaveと同じ「ユーザーの`main`を`!amifl_main`という別名でコンパイルし、`!main`は`amifl_main`を呼んで`os.Exit()`に渡す薄いラッパー」という設計にした（詳細は`CLAUDE.md`「確定した設計判断」参照）。

素朴に「`amifl_main`の戻り値`%code`（`^int64`）をそのまま`os.Exit`に渡す」IRを書くと、`amivm`のIRパース自体は通るが、生成されたGoコードの`go/types`型チェックで`cannot use ... (variable of type int64) as int value in argument to os.Exit`という形で初めて発覚する。**GoのAPI（`os.Exit`はプラットフォーム依存の`int`を取る）と対象言語の固定幅整数型（AmiFLの`Int`＝`Int64`）が食い違う境界は、IRの構文チェックだけでは検出できず、必ず`go build`まで通して確認する必要がある**——という、Seed/Cascade/Weaveのnotesが繰り返し強調する「実地検証必須」の教訓を、`main`ブリッジという最も基本的な部分でさっそく再確認した形になった。

対策は`CALL`のキャスト機能（Goの型変換`T(v)`と`ast.CallExpr`が構文的に同一であることを利用）を1回挟むだけ：

```
VAR	%code	^int64
VAR	%exitCode	^int
CALL	%code	:	!amifl_main
CALL	%exitCode	:	?int	%code
CALL	:	?os.Exit	%exitCode
```

**一般化した教訓**: AmiFLの固定幅数値型（`Int8`〜`UInt64`等）をGoの標準ライブラリ関数（`os.Exit`に限らず、`int`を要求する他のAPI全般）へ渡す境界では、常にこの「AmiFL型→Go APIが要求する型」への明示キャストが必要になる可能性を意識すること。今後`extern`機構（15節）でGo資産をバインドする際、引数・戻り値の型変換ロジックとしてこのパターンを一般化できる見込み。

### ブロック内の式区切りは改行トークン（Weave方式を採用、仕様の未決事項を解消）

`amifl-spec.md`は式の区切り方法を明記していなかった（原則1「プログラムは式の並びのみ」とあるだけ）。Weaveの`weave_implementation_notes.md`が示す「レキサーが実際の`\n`をそのまま`Newline`トークンとして生成し、パーサーがブロック内でだけそれを区切りとして消費する」という設計（Goのセミコロン自動挿入のような中間層を持たない、最も単純な形）をそのまま採用した。実装・実地検証とも問題なし。詳細は`CLAUDE.md`「確定した設計判断」参照。

### amivmの未使用変数自己修復が、AmiFLの`_ = expr`設計をそのまま裏付けた（Step 2で実地確認）

`ignored/amivm/CLAUDE.md`に「amivmは生成コード中の未使用変数を自己修復する仕組み（`_ = 変数名`の自動挿入）を内部に持っている」という記述があったが、実際に`let inferred = 42`（以降未使用）を含むAmiFLプログラムを`emit-go`で確認したところ、生成されたGoコードには`var ...inferred int64`の直後に`_ = ...inferred`が自動的に挿入されており、`go build`が問題なく通ることを確認した。

**一般化した教訓**: 対象言語のcodegenが「このローカル変数は後で使われるだろうか」を心配する必要は無い——amivmが最終防衛線として引き受けてくれる。この事実は`_ = expr`（AmiFLの明示的な値破棄構文）の設計判断にも直接影響した：`_ = expr`はGoの未使用変数エラーを回避するための構文ではなく、**AmiFL自身の「ブロック内の最後以外の式はUnit型必須」という言語規律を満たすためだけの構文**だと判明した。そのためcodegen側は「副作用の無い値（リテラル・変数読み取り）を`_ = ...`で捨てる」ケースでは一切IRを生成しない——Goの未使用変数対策としての`_ = x`をAmiFL側で二重に生成する必要はなく、amivmに完全に委ねてよい。この整理をCascadeの同種の結論（cascade_implementation_notes.mdではなくCascade自身のCLAUDE.md）と照合し、3言語目（Cascade）・4言語目（AmiFL）で独立に同じ結論へ達したことを確認できた。

### 数値リテラルは「無型」として文脈の期待型へ適応させる設計にした（先行実装に無い、AmiFL独自の設計判断）

Seed/Cascadeはどちらも静的型付けだが、リテラルが「無型」かどうかという設計判断そのものへの直接の言及は無かった（両言語とも、リテラルの型は構文上ほぼ自明だったため、この設計判断を明示的に迫られなかったと見られる）。AmiFLは`let`/`const`の型注釈が省略可能で、かつ数値型のサイズ・符号違いが暗黙変換不可（原則2）という組み合わせのため、**リテラル自体は「まだ型が確定していない値」とし、使われる文脈（`let`の型注釈、無ければデフォルト`Int64`/`Float64`）に応じて初めて具体的なAMIVM型が決まる**という設計にした（Goの無型定数の考え方に近い）。

**この設計はコード生成側に嬉しい副産物をもたらした**: リテラルトークンをAMIVM-IRへ出力する際、常に生の数字列（`42`や`3.14`）をそのまま埋め込むだけで良く、対象の変数がどの具体的な数値型であっても、Go自身の無型定数→具体型への暗黙変換規則がそのまま働く（`var x int8; x = 200`のような範囲外だけがGo自身の`go/types`でも弾かれる——ただし「わかりやすいAmiFL自身のエラーで先に弾く」という意味検証の責任分担の原則により、AmiFL側でも同じ範囲チェックを`internal/sema/expr.go`の`resolveIntLit`で先に行っている）。

**唯一の罠**: 浮動小数点リテラルをAMIVM-IRへ出力する際、Goの`strconv.FormatFloat`が整数値ぴったりの浮動小数点数（例: `5.0`）を小数点無しの`"5"`という文字列にしてしまう。これ自体はGoのビルド結果としては無害（無型定数`5`は`float64`変数へ問題なく代入できるため）だが、IRやGoソースを目視で読んだときに「本当にfloatとして生成されているのか」が分かりにくくなる。対策として`internal/codegen/codegen.go`の`formatFloatLit`が、小数点を含まない場合は`.0`を付与するようにした（Weaveの`weave_implementation_notes.md`にある「整数形の値には強制的に`.0`を付与し、Goの無型浮動小数点定数のデフォルト型`float64`を選ばせる」という教訓と同じ発想——ただしWeaveの動機は`any`型変数がint型のデフォルトを選んでしまう問題の回避であり、AmiFLの動機は可読性のみという違いがある）。

### 二項演算子の両オペランドへ「無型リテラル」の適応をどう広げるか（Step 3で発見、先行実装に無い罠）

Step 2の「リテラルは無型」設計を二項演算子（`a op b`）へそのまま拡張しようとすると、素朴な「左から先に解決し、右を左の型に合わせる」実装は**非可換なバグ**を埋め込む。`count + 1`（`count: Int8`）は正しく`Int8`同士として通るが、`1 + count`はリテラル`1`が先に解決されてしまい期待型が無いままデフォルト`Int64`に固定され、後続の`count`（`Int8`）との不一致で不当なエラーになる——数学的には可換な`+`のはずなのに、ソース上の書き方次第でコンパイルが通ったり通らなかったりする、実利用者が最も混乱しやすい種類のバグ。

対策は「オペランドの型解決順」を左→右の構文順ではなく「**具体型が決まっている方を先に解決する**」という優先順位に変えたこと（`internal/sema/expr.go`の`resolveOperandTypes`）。リテラルかどうかは構文ノードの種類（`IntLit`/`FloatLit`かどうか）だけで判定でき、`const`参照は（Step 2の時点でリテラルではなく`IdentExpr`ノードなので）「具体型が決まっている方」として扱われる——`const`はリテラルへ後から適応することはできない、という暗黙の仕様判断も同時に確定した形になる。

### `-`（単項マイナス）はAMIVM命令に存在しない——リテラルは符号付きトークン、それ以外は`0 - x`で表現する（Step 3で発見）

CLAUDE.mdの「AMIVM-IRの書き方」節にある命令一覧を素直に見ると、算術命令は`ADD SUB MUL DIV MOD`の二項演算子しかなく、単項の`NEG`に相当する命令が無い。Bitwiseの`BNOT`・論理の`NOT`という単項命令はあるのに、算術の単項マイナスだけ抜けている——実装前は見落としがちな非対称さ。

解決の鍵は`ignored/amivm/amivm_spec.md` 5節のオペランドカテゴリ表だった：`integer`/`number`カテゴリは`0`,`1234`だけでなく`-1234`という符号付きトークンも直接許容している。つまり**AMIVMの数値リテラルは元から符号付きで書ける**——単項マイナスの適用対象がリテラル（またはリテラルへ還元できる`const`参照・二重否定の連鎖）である限り、命令は一切不要で、符号を畳み込んだトークン文字列を埋め込むだけでよい。対象が変数や別の式の結果（リテラルへ還元できない値）である場合だけ、AMIVMにある唯一の減算命令`SUB`を使って`SUB %tmp 0 operand`と展開する。

**一般化した教訓**: 「対象言語に無い単項演算子」を見つけたら、まず該当カテゴリの二項演算子（この場合`SUB`）へ`0`を補って合成できないか、そして次にオペランドカテゴリ表がリテラルへの符号直接埋め込みを許していないか、の2段階で確認するとよい。後者が使える場面（リテラル・定数畳み込み）では命令コストがゼロになる。

### `const`の初期化式に演算子が絡む場合、コンパイル時定数畳み込みは実装しない——参照箇所ごとに再生成する（Step 3で確定・CLAUDE.md参照）

`const X = A + B`を実装する際、最初は「sema側でGoの算術を模倣し、`X`を単一のリテラルへ畳み込む」方式を検討したが、AmiFLの整数型は幅・符号ごとに異なるラップアラウンド規則を持つため、この畳み込みをsema内に実装するとGoの算術演算の意味論をAmiFL側で二重に持つことになる。代わりに採った方式（型検査済みの初期化式の木をそのまま保持し、参照のたびに`codegen`が同じ命令列を再生成する）の設計理由・トレードオフは詳細をCLAUDE.md「確定した設計判断」の該当節に譲る。ここでは一般化した教訓だけ書く：**「コンパイル時定数畳み込み」と「実行時ストレージを持たない」は別の要求であり、後者だけなら前者を実装しなくても満たせる**（生成先言語＝Goの算術演算をそのまま信頼できるなら、なおさら）。

### ブロックが本物のGo入れ子ブロックへ対応するなら安全、という思い込みは誤りだった——amivmの未使用変数自己修復は「関数内で変数名は一意」という別の前提を持っている（Step 4で発見・最重要）

Step 4で`if`/`while`本体にレキシカルスコープ（`let`のシャドーイング）を導入する際、「AMIVMの`IF`/`LOOP`は本物のネストしたGoブロックを生成する（goto経由の平坦化ではない）」という事実を確認していたため、「だったらcodegen側は一切リネームせず`%変数名`をそのまま使い続けても、Goのブロックスコープが自然にシャドーイングを解決してくれるはずだ」と判断し、その設計で一度実装した。

しかし実際に`let x = 1; if true { let x = 2; print("ok") } \n x`（内側の`x`が未使用）というプログラムを`amivm`→`go build`まで通したところ、`amivm`が`Error: unused variable resolution reached the retry limit (5 attempts)`で失敗した。原因はamivmの未使用変数自己修復機構（`cmd/amivm/compile.go`の`findAndInsertBlank`）が、未使用と報告された変数名を**名前だけを手がかりに**ASTから探し、最初に見つかった宣言の直後へ`_ = 変数名`を挿入するという実装になっていたこと——「関数内で同じ名前の`VAR`宣言は1つしか無い」という前提に立っている。AmiFLが同じ`%x`を2箇所（外側・内側）で宣言すると、amivmは常に最初に見つかる方（このケースでは既に使用済みの外側の`x`）を「未使用」だと誤認して`_ = x`を挿入し続け、本当に未使用な内側の`x`は永遠に直らないまま同じエラーが再発し、リトライ上限で失敗する。

**一般化した教訓**: 対象言語のcodegenが依存する既存ツール（ここではamivm）の「自動修復・自己解決」系の機能は、それ自体が暗黙の前提（この場合「関数内で変数名は一意」）を持っていることがある。対象言語側の新機能（この場合シャドーイング）がその前提を破る場合、生成先の言語（Go）レベルでは合法でも、ツールの補助機能が壊れて全体が失敗する——「Goとして正しいコードを生成できているか」だけでなく「利用しているツールの暗黙の前提を破っていないか」も実地検証すべき対象になる。この手のバグは机上の検討だけでは決して見つからず、実際に`amivm`→`go build`のフルパイプラインを通して初めて発覚した（Seed/Cascade/Weave notesが繰り返し強調する「実地検証必須」パターンが、Step 4でも新しい形で再確認された）。

**対策**: Cascade（`freshName`）と同じ、関数全体で共有する1つの通し番号カウンタから`name_N`という一意な内部名を毎回発行する方式に切り替えた。「AMIVMのブロックが本物のGoブロックだから対象言語側の工夫は不要」という判断は、あくまで対象言語(Go)自身の構文規則の話であり、ビルドパイプライン上の他のツール（この場合amivm）が持つ別の制約まではカバーしないと肝に銘じること。

### `FUNC`/`CLOS`の見出し行に引数トークンが一切登場しない、という気づきがcodegenを大きく単純化した（Step 5で発見、先行実装との違い）

`FUNC defname type1 type2 ... : type3 ...`も`CLOS single1 type1 type2 ... : type3 ...`も、引数リストは**型だけ**を位置で列挙し、`$1`/`&1-1`のような引数名（トークン）自体は見出し行のどこにも書かない。トークンが登場するのは本体内の**参照**箇所だけ。これに気づいたことで、「codegenが今CLOSの何階層目を生成中か」を追跡するカウンタが一切不要になった——sema側が変数解決の時点で（`&depth-N`という完全修飾トークンを）1回だけ計算し、`ast.IdentExpr`に焼き込んでしまえば、codegenはただそれを読んで出力するだけでよい。Cascadeの`cascade_implementation_notes.md`（クロージャー・高階関数の設計）はcodegen側に`closureDepth`相当の状態を持たせていたが、これはCascadeのcodegenが「goto/VAR巻き上げ方式」（関数ごとに独立した`decls`を持ち、関数の先頭でまとめて`VAR`を出力する）を採用していたための必要性であり、**AMIVMのIF/LOOP/CLOSが本物のネストしたGoブロックである**という前提に立つ設計（AmiFLがStep 4で確定させた方式）では、この手の「今どの階層にいるか」の状態はsemaだけが持てば足り、codegenには波及しない。

**一般化した教訓**: 生成先の中間言語命令（ここでは`FUNC`/`CLOS`）の構文を読むとき、「引数の型」と「引数の名前（トークン）」が同じ見出し行に同居しているか、別々の場所（見出し行 vs 本体の参照）に分かれているかを確認する価値がある。分かれている場合、「名前解決に必要な状態」を作る責任と「名前を消費するだけの責任」を完全に分離でき、後者（codegen）を大幅に単純化できる可能性がある。

### `let`の値がクロージャーリテラルの場合、`VAR+SET`ではなく`VAR`+`CLOS`という別パターンに乗る——「genValueが値トークンを返す」という既存の統一パターンの外側にいる式が初めて出てきた（Step 5で発見）

Step 1〜4のあらゆる式（リテラル・二項演算・if式等）は「`genValue`が値トークン（`%tmp`等）を返し、呼び出し側がそれを`SET`する」という完全に統一されたパターンに乗っていた。`CLOS single1 ... : ...`だけはこのパターンに乗らない——`single1 = func(...) {...}`という**代入文そのもの**を生成する命令であり、「まず値を計算してから代入する」という2段階に分解できない（Goの関数リテラル式自体は値だが、AMIVMの`CLOS`命令はその値を計算する式と代入を1つの命令に統合している）。このため、クロージャーリテラルだけは`genValue`の一般ディスパッチに乗せず、`genLetStmt`が値の型を見て`*ast.ClosureLit`なら専用関数（`genClosureLitInto`）へ迂回させる、という特別扱いが必要だった。

**一般化した教訓**: 中間言語のある命令が「値の計算」と「代入」を1つの命令に統合している場合（`CLOS`のように、対象言語では"式"だが生成先中間言語では"文"寄りの命令になっている場合）、フロントエンド側の「あらゆる式はgenValueが値トークンを返す」という統一的な抽象化に無理に押し込めようとせず、該当する式の種類だけ最初から特別扱いする設計にした方が、コード生成ロジック全体がむしろ単純になる。

### `Func`型は「ユーザーが書ける型注釈構文」まで実装しなくても、コンパイラ内部の型として十分な検査能力を持たせられる（Step 5で確定、AmiFL独自の設計判断）

`amifl-spec.md`が`Func`型の記法（`fn(T1,...) -> R`）を明記していても、それをただちに「型注釈として書ける構文」として実装する必要は無いと判断した——唯一の仕様例（`let square = fn(x: Int) -> Int {...}`）自体が型注釈を省略しており、クロージャーリテラル自身が引数・戻り値を常に完全に明示する構文である以上、「クロージャーの型は常にクロージャー自身の宣言から一意に決まる」という前提を崩さない限り、外部からの型注釈（＝型注釈のパーサー拡張、内部表現の構造化、既存注釈との一致検査）は一切不要——クロージャーが`let`の直接の値としてしか使えない（呼び出し引数や別の`let`への代入元にはなれない）という制約と引き換えに、`Func`型を「呼び出し時の引数個数・型・戻り値型を検査する」という核心機能だけの、コンパイラ内部限定の文字列エンコーディング（`"fn(P1,P2)->R"`、決してパースされない、常に自分自身の`makeFuncType`が組み立てたものだけを`funcTypeParts`が読み戻す）として実装できた。

**一般化した教訓**: 言語仕様が「値の型」として何かを明記していても、その型の**ユーザー向け表記構文**まで実装するかどうかは独立した設計判断にできる。「その型の値が実際にどこから来て、どこで使われるか」という利用パターンを先に絞り込むことで、構文実装の労力を丸ごと先送りしつつ、型検査の実質的な効果（コンパイル時に引数の数・型を検査する等）は損なわずに済むケースがある。

### 「型ごとに発行、同じ形なら使い回す」という中間のキャッシュ戦略が、クロージャーの「毎回新規発行」とタプルの「形で重複排除」という2つの異なる要求を同じ`program`構造体の枠組みで両立できた（Step 6で発見）

Step 5のクロージャー用`FNTYPE`発行（`program.newFuncTypeDecl`）は「呼ばれるたびに無条件で新しい型名を発行する」という最も単純な方式だった——クロージャーの型はユーザーの目に触れる注釈と一致させる必要がそもそも無かったため、重複排除の恩恵が無かった。Step 6のタプル用`STTYPE`発行（`program.tupleGoTypeName`）は逆に「同じ形（同じ要素型の並び）なら同じGo型を返す」というキャッシュ付きの方式が必要だった——タプルの等値比較や、将来関数シグネチャを介して同じ形のタプルが行き来する場面を考えると、形が同じなのに違うGo型になってしまうと破綻するため。この2つの異なる戦略は、どちらも「`program`という1つのGenerate呼び出し全体で共有される状態」に、それぞれ独立したフィールド（`closureSeq`という単純カウンタ vs `tupleTypes map[string]string`というキャッシュ）を持たせるだけで両立できた——共通の「型ヘッダーへ追記する」という土台（`typeHeader strings.Builder`）は完全に共用しつつ、「いつ新規発行するか」の判断だけを呼び出し側ごとに変えられる、という設計の柔軟性が効いた形。

**一般化した教訓**: 中間言語の「型を先に宣言してから使う」という命令（`FNTYPE`/`STTYPE`のような）を複数の言語機能（クロージャー・タプル等）が使う場合、それぞれの機能が「型の同一性」に対して異なる要求を持ちうる（生成のたびに新規でよいか、形が同じなら共有すべきか）。型ヘッダーを蓄積する共通の入れ物と、型名を発行する判断ロジックを分離しておくと、後から追加する機能が異なるキャッシュ戦略を要求しても、入れ物側の設計をやり直さずに済む。

### AST側で「タプル添字とstructフィールド名は同じノード」に統一したことで、パーサー・codegen双方から「今どっちを処理しているか」の判定コードを完全に排除できた（Step 6で発見）

`t.0`（タプル添字）と`p.x`（structフィールド）は表面上の構文（後置`.`）も生成先命令（`FGET`）も同じであり、両者を区別する情報（「Targetの型がタプルかstructか」）は静的に一度だけ、sema側で確定する。この事実に着目し、パーサーは`.`の後のトークンが数字か識別子かを一切気にせず両方とも生の文字列として1つの`FieldExpr.Field`へ格納し、semaだけが型を見て初めて「タプル添字なら`F`+インデックスへ変換、structフィールドならそのまま」という1回限りの判定を行い、その結果（`AmivmField`）をASTへ焼き込む。codegen側はこの判定ロジックを一切持たない——sema が計算済みの`AmivmField`をそのまま`FGET`の第3オペランドへ書くだけ。

**一般化した教訓**: 対象言語の複数の構文要素が、意味的には異なる概念（タプル添字とフィールド名）でも生成先の命令が完全に同一になる場合、AST側で無理に別ノードへ分けず、1つのノードに「表層のテキスト」と「意味解決後の生成先トークン」という2つのフィールドを持たせ、後者をsemaが1回だけ計算する設計にすると、以降のフェーズ（codegen）がその区別を二度と気にしなくてよくなる。これはStep 5で確立した「semaが計算した最終形をASTへ焼き込み、codegenは読むだけ」というパターン（`CalleeToken`/`Token`）の直接の拡張で、複数の異なる意味論を持つ構文が同じ生成先命令に収束する場面では特に効果が大きい。

### 対象言語のif/for条件式に複合リテラル構文を後から追加すると、既存の「条件直後の`{`」パースが曖昧になる——Goと全く同じ問題なのでGoの解決策(`exprLev`相当のフラグ)がそのまま使える（Step 6で発見・事前に警告されていた課題の実地対応）

`struct`リテラル（`Name{...}`）を導入する前は、`if cond { ... }`の`cond`の直後の`{`は常にif本体の開始だと確定的に判定できていた。`struct`リテラルを導入した瞬間、`cond`がちょうど「識別子」で終わる形（`if flag { ... }`）だと、パーサーは「`flag`は変数か、それとも直後の`{`から始まるstructリテラルの型名か」を構文だけでは判定できなくなる（型はまだ分かっていない——静的型付け言語でも、パース時点ではまだ型検査前）。この曖昧性は複合リテラル構文を持つどの言語でも発生しうる普遍的な問題で、Goは`exprLev`という「今、複合リテラルを構文的に許可してよい文脈にいるか」を表すパーサー内部の状態で解決している。AmiFLも全く同じ手法（`parser.noCompositeLit bool`、条件式パース中だけ`true`、明示的な閉じトークン`(`/`{`/呼び出し引数リストに入るたびに`false`へ退避）でそのまま解決できた。

**一般化した教訓**: 複合リテラル構文（`TypeName{...}`のような、識別子の直後に`{`が来る形）を持つ言語で、かつif/for/while等の条件式の直後に`{`でブロックが始まる構文を持つ場合、この曖昧性は構文設計上ほぼ不可避——複合リテラルの導入前に気づいて事前に設計判断を残しておく（AmiFLはStep 4の`parseIfExpr`のdocコメントで先回りして警告していた）と、実装時に慌てず対応できる。解決策自体はGoの`exprLev`という20年近く実証されている手法をそのまま輸入すればよく、独自に考案する必要はない。

### 「裸の型名しか受け付けない文脈」がAMIVMのあちこちに散らばっている——複合形の型トークンを一度でも許すと、その周りの全ての命令の制約を洗い出す羽目になる（Step 7で発見）

`VAR`の型オペランドは`^[n]type1`という無名の複合形を直接サポートするが、`STTYPE`の`FIELD`・`SLTYPE`の要素型・`ARTYPE`自身の要素型など、型を受け取る他の命令の多くは「裸の`^xxx_123`一つ」しか受け付けない（複合形を差し込めるかどうかは命令ごとに暗黙に決まっていて、`amivm_spec.md`のオペランドカテゴリ表を読んだだけでは全命令の組み合わせまでは確認しきれない）。最初は「1次元配列だけは複合形で済ませ、多次元の時だけARTYPEで名前を付ける」という最小実装を検討したが、これだと`resolveGoType`（コード生成側の型解決の一本化関数）が「今この型は複合形を許される文脈で使われているか」を呼び出し側ごとに把握する必要が出てきて、Tuple/List/structの「常に裸の型名を返す」という統一契約が崩れる。

**対策**: 1次元でも複合形を使わず、**常に名前付きの型（`ARTYPE`）を発行する**という統一方針に倒した。命令1つ分（`ARTYPE`の1行）のIR冗長性と引き換えに、`resolveGoType`を「どんなAmiFL型を渡しても常に裸の型名を返す」という単純な契約に保てた——複合形が使える文脈・使えない文脈を洗い出すコストより、常に名前を付けるコストの方が圧倒的に低い。

**一般化した教訓**: 中間言語が「ある種類の型トークンには無名の複合形と名前付き宣言の両方がある」という設計になっている場合、複合形が使える文脈と使えない文脈が命令ごとにバラバラである可能性を疑うこと。コード生成側の型解決ロジックを「常に名前付き」に統一できるなら、複合形は積極的には使わない方が、結果的に実装が単純になることが多い。

### `genValue`が「複合式は常に新しいコピーを作る」という規律を持つ言語では、複合代入（`x[i]=v`のような）のTargetが入れ子になった瞬間、値渡し型と参照型の違いが表面化する（Step 7で発見・「過去に踏まれた地雷」#8の再確認）

AmiFLのcodegenは一貫して「式を評価した結果は常に新しい一時変数（コピー）」という規律で書かれている（`genValue`の統一契約）。単純な代入（`x[i]=v`、`x`が裸の変数）ではこれは何の問題も起こさない——`x`自体はコピーせず直接使うため。しかし**Targetがそれ自体`x[i][j]=v`のように入れ子になった瞬間**、内側の`x[i]`を`genValue`経由で読み出すと必ずコピーが生まれる。このコピーへ書き込んでも元のストレージ（`x`の中身）へは反映されない——**ただし、これはGoのスライス（参照型、ヘッダーのコピーでも同じ裏配列を指す）では偶然問題にならず、Goのネイティブ配列（値型）でだけ本当に問題になる**、という非対称性が罠だった。List専用のテストだけで進めていたら「動いているように見える」状態で見逃していたはずで、Arrayも同じコードパスに乗せて初めて発覚した。

**対策**: ADDR命令（単一階層のフィールド/添字までしか対応しない）に頼らず、**再帰的な読み出し→変更→書き戻し**（`x[i]`を`AGET`で読み出し→変更→`ASET`で書き戻し→さらにそれ自体が`y[j]`のようなインデックス式ならもう一段再帰）で対処した。この方式はADDRの「単一階層まで」という制約を気にする必要が無く、任意の深さのネストに対応でき、List・Array・両者混在のいずれでも値渡し/参照渡しの違いを気にせず正しく動く。

**一般化した教訓**: 対象言語の複合代入先（lvalue）が任意の深さでネストしうる場合、「一時変数を経由した読み出しは必ずコピーになる」という前提のもとで、**書き込みは必ず最終的に「元のストレージへの言及」まで遡って書き戻す**設計にする必要がある。値型・参照型が混在する型システムでは、参照型だけでテストして「動いた」と判断すると、値型のケースで静かに壊れる作りを見逃す——両方の型を同じコードパスでテストすることが重要（Seed/Cascade/Weave notesの「実地検証必須」の教えを、今回は「両方の代表的なケースを実地検証する」という形で再確認した）。

### `continue`が壊れる問題への対策（Weave由来の「先に済ませる」パターン）を、`for`ループの添字管理でも再利用できた（Step 7で確認）

CLAUDE.mdの「過去に踏まれた地雷」#3（`continue`は`LOOP`の先頭へ戻るため、本体の後に必須の処理があると壊れる）は元々Seed/Cascade（`LABEL`/`GOTO`で専用continueラベルを作る）とWeave（添字更新を先頭に移動する）という2通りの対策が申し送られていたが、AmiFLの`for`実装でも同じ選択に直面し、Weave方式をそのまま採用できた——添字を`-1`で初期化し、ループ本体に入る前に必ず`+1`してから使う、という一手間で、`LABEL`/`GOTO`を一切使わずに済んだ。

**一般化した教訓**: 「ループ本体の後に必須の後処理がある」という設計そのものを避け、「後処理を次のループの先頭で最初に行う（かつ初期値をその分だけずらす）」という形に倒せないか、実装前にまず検討する価値がある——`GOTO`ベースの解決より一貫して単純で、Seed/Cascadeの先行実装がある領域でも「もっと良い方法が無いか」を確認する意義があることの実例。

### タグ付き共用体（`enum`）は「複数の`STTYPE`+インターフェース」ではなく「1つの`STTYPE`へ全バリアントのフィールドを統合し、`Tag`で判別する」設計の方が、AMIVMの命令セットと素直に噛み合った（Step 8で確定）

`enum`の実行時表現には大きく2つの方針が考えられた：(a) バリアントごとに別々の`STTYPE`を発行し、何らかの共通インターフェース（`INTYPE`）または`Any`相当の箱でくるむ、(b) 1つの`STTYPE`に`Tag`フィールドと全バリアントの全フィールドを（フィールド名をバリアント名で修飾して衝突を避けつつ）まとめて持たせる。(a)はGoの`interface{}`的な動的ディスパッチに近い形になり、AMIVMの`ASSERT`（型アサーション）や`INTYPE`が必要になる——しかしCLAUDE.mdが元々「AmiFLはユーザー定義capability拡張が無く、`INTYPE`の出番は無い見込み」と申し送っていたとおり、`enum`のバリアント判定は`switch`だけに限定されている（`amifl-spec.md`「バリアント判定・フィールド取り出しはswitchのパターンマッチでのみ行う」）ため、動的ディスパッチの機構そのものが要らない。(b)を選んだ結果、`STTYPE`/`FIELD`/`FSET`/`FGET`という既存命令だけで完結し、`switch`のパターンマッチも「`Tag`をバリアントの宣言順位置と`EQ`で比較する`IF`/`ELSE`チェーン」という、既存のif/elif desugaring（Step 4）の直接の拡張で済んだ。

**フィールド名の衝突回避**：Goの構造体は1つの型に全フィールドをフラットに持つため、バリアントAの`delay`とバリアントBの`delay`が同じenumに（仮に）存在すると素朴には衝突する。`<バリアント名>_<フィールド名>`という合成名（enum内でバリアント名は一意なので、これだけで衝突が起こりえない）で解決した——Step 6のタプルが`.0`/`.1`という裸の数字をGo識別子にできず`F0`/`F1`という合成名にした判断と同じ発想（「対象言語の表層の名前をそのままGo識別子として使えない場面では、衝突しないことが構造的に保証される合成規則を作る」）の直接の応用。

**「値ではなくタグで判別する」ことに気づいたのが設計の分岐点だった**：Goのネイティブ構造体はデフォルトで値のゼロ初期化を持つため、「今どのバリアントとして構築されたか」というタグさえ別途持たせれば、使われていない他バリアントのフィールドはただのゼロ値として無害に居座り続ける（読み出されることが無いので実害が無い——`switch`のcase本体は必ず対応する`Tag`が一致した分岐の中でしか該当フィールドを`FGET`しないため）。これはGoの共用体的な使い方（`C`言語の`union`のような領域再利用）ではなく、単なる「使わないフィールドを持て余す」という素直な設計で、複雑さが無い代わりにメモリ効率は最適ではない——バリアント数・フィールド数が大きくなる場合はこのトレードオフを再検討する価値があるが、Step 8時点では実装の単純さを優先した。

**一般化した教訓**：対象言語の「タグ付き共用体」を、生成先言語がネイティブな共用体機構を持たない場合（Goがまさにそう）に実装するときは、まず「バリアント判定・フィールド取り出しの手段がどこまで公開されているか」（言語仕様がどこまで動的な操作を許すか）を確認すること。判別が専用構文（`switch`等）だけに限定されているなら、動的ディスパッチ・インターフェース的な機構を持ち出さず、「タグ+全フィールド統合」という最も単純な表現で足りることが多い。逆に「任意の場面でバリアントを問わず扱える汎用値」（動的型付け言語の`Any`的な扱い）まで要求される場合は、この単純化は使えず、インターフェース/`ASSERT`ベースの設計が必要になる。

### `Unit`型（実行時表現を持たない型）を「値」として`genValue`経由で扱おうとすると、存在しないGo型名がそのままIRへ漏れる——単体テストが実際にこの壊れたIRを再現して初めて気づいた（Step 9で発見）

AmiFLの`Unit`型は実行時表現を一切持たない（`internal/codegen`の`goTypeNames`に対応エントリが無い）。これまで`Unit`型の値（`print`呼び出し等）が`genValue`（値として評価する経路）に渡ることは無かった——`Unit`型の式は常に文位置（`genStmt`経由）でしか現れないというsema側の制約（ブロックの非末尾式は`Unit`必須、`let`は`Unit`束縛禁止等）により、自然と守られていた不変条件だった。

Step 9の`for...yield`実装で、`Yield`式を無条件に`genValue`で評価するコードを書いた際、この不変条件を初めて破ってしまった——`Yield`が`Unit`型（`print(...)`のような呼び出し）でも文法上は妥当に見えたため、sema側に専用のガードを追加し忘れていた。単体テストで実際に「`Yield`が`Unit`型のケース」を書いてみたところ、`resolveGoType`が未知の型名（"Unit"という文字列そのもの）をフォールバックでそのまま返し、`VAR %tmp ^Unit`という**Goに存在しない型名を含むIR**が生成されることが判明した——これは`amivm`の`go/types`検査で初めて発覚するはずだった壊れたコードで、テストを書いて実際に生成IRを目視するまで気づかなかった。

**対策**：sema側（`resolveForExpr`）に「`Yield`が`Unit`型なら拒否」という明示的なチェックを追加し、`genValue`が`Unit`型の式を受け取ることが二度と起こらないようにした。

**一般化した教訓**：ある型が「実行時表現を持たない」という設計（AmiFLの`Unit`、他言語の`void`相当）を採用している場合、その型の値が「値として評価される」経路（`genValue`相当）に到達しうる**新しい構文要素**を追加するたびに、既存のsemaの制約（ブロックの末尾式・`let`束縛禁止等）がその新しい構文要素にも及んでいるかを明示的に確認すること。既存の制約は「今までに存在した構文要素の組み合わせ」に対しては効いていても、新しい構文要素（今回の`yield`）が制約の網を素通りしてしまう可能性は毎回チェックする必要がある——「これまで問題が起きなかったから大丈夫」という類推は、新しい構文要素が既存の経路と違う組み合わせでコード生成に到達する場合には通用しない。単体テストで実際に生成されたIRを目視する習慣（CLAUDE.md「実地検証必須」）が、まさにこの種の見落としを捕まえる最後の砦になった。

### コンパイラ内部の型エンコーディング文字列に「入れ子になった型」を埋め込む場面が初めて出ると、単純なカンマ/セミコロン分割では壊れる（Step 10で発見）

Step 5〜7で確立した「型をカンマ区切りの正規化文字列としてエンコードする」パターン（`Func`型の`"fn(P1,P2)->R"`、Tuple型の`"Tuple(T1,T2,...)"`、Array型の`"Array(elem;size)"`）は、いずれも**内部要素がカンマ/セミコロンを含まない**という暗黙の前提の上に成り立っていた——Tuple/FuncはネストしたTuple/Funcを禁止する設計（Step 5/6のスコープカット）にしたことで、要素側にカンマが紛れ込む可能性そのものを構造的に排除していた。Array[T;N]も、ネストしたArrayが持つ内側の`;`と外側の`;`を「最後の`;`」で確実に分離できる（内部要素がセミコロン以外の区切りしか使わないため）という別の理由で安全だった。

Step 10のMap[K,V]で初めてこの前提が崩れた——Vは仕様上どんなコレクション型にもなれる（`Map[String, List[Int]]`のように）ため、正規化文字列の内部に別の`"List(...)"`や`"Map(...)"`がまるごと埋め込まれる。素朴な「最初のカンマで分割」「最後のセミコロンで分割」はどちらも通用しない——**深さを数えながら、深さ0の区切り文字でだけ分割する**という、一段階複雑なパーサーが必要になった。

**一般化した教訓**：対象言語のある型構築子（このケースではMap）が、他の型構築子と違って**内部要素の型を無制限に許す**（ネストを禁止しない）設計になっている場合、その型のためのコンパイラ内部エンコーディングは「区切り文字の単純な検索」では足りない可能性を疑うこと——先行する型構築子（Tuple/Func/Array）がどれも意図的にネストを禁止していたためにこの問題を回避できていた、という事情に気づかないまま同じ分割ロジックを使い回すと、ネストしたケースで静かに間違った位置を切ってしまう（`go build`まで通ってしまう可能性すらあり、実行時の型不一致という形でしか発覚しないかもしれない、より発見しにくいバグになりうる——今回はコードレビューの時点でこの非対称性に気づき、実装前に対策できた）。

### 「関数だけで完結するリテラルマップ」でも、参照先の関数が自分自身を間接的に呼び戻す構造だと初期化順序サイクル扱いになる（Step 11で発見）

`sema/builtins.go`の組み込み関数レジストリを`var builtinFuncs = map[string]builtinResolver{"isError": resolveIsError, ...}`という素朴な`var`初期化で書いたところ、`go build`が`initialization cycle for builtinFuncs`というエラーを出した。原因は、`resolveIsError`（マップリテラルが参照する関数値）の本体が`checkExpr`→`resolveType`→`resolveCallExpr`→`resolveBuiltinCall`と辿って**最終的に`builtinFuncs`自身を再度参照する**経路を持っていたこと——Goのパッケージレベル変数の初期化順序解析は、「ある変数の初期化式が参照する関数の**本体まで再帰的に辿り**、そこで参照される別の変数も依存先とみなす」という仕様になっており（単なる字面上の直接参照だけでなく、間接的な関数呼び出しの依存関係まで含めて解析する）、実際には`Check()`実行時（パッケージ初期化がとっくに終わった後）にしか評価されないマップの中身であっても、コンパイル時の依存グラフとしては自己参照サイクルに見えてしまう。

対策は`var builtinFuncs map[string]builtinResolver`（ゼロ値のみ）と宣言だけしておき、実際のマップリテラルの構築を`func init() { builtinFuncs = map[string]builtinResolver{...} }`へ追い出すこと——`init()`関数の中身はGoの初期化順序解析の対象外（あくまで「パッケージレベル変数の初期化式」だけが解析対象）なので、同じ依存関係があってもサイクルとして扱われない。

**一般化した教訓**：「複数の関数を1つのマップ/スライスへ集約するレジストリパターン」を書く際、レジストリに登録する関数群が（直接であれ、他の関数を経由した間接であれ）**レジストリ自身を参照し返す**可能性がある設計（今回のような「組み込み関数の引数チェックが、別の組み込み関数呼び出しを再帰的にチェックしうる」という状況は、ネストした式を扱うコンパイラでは自然に発生する）なら、`var`の直接初期化ではなく`init()`に構築ロジックを追い出すことを最初から検討すること。エラーメッセージ自体（`initialization cycle for X: X refers to Y ... Y refers to X`）は原因を正確に指しているため、実地検証（`go build`）さえ通せば発見自体は容易だが、「なぜ変数の初期化が関数呼び出しの中身まで見るのか」という理屈を知らないと対策（`init()`への追い出し）に辿り着きにくい。

### 型注釈構文の実装漏れは、その型を**返す**関数を書いて初めて発覚することがある（Step 11で発見）

Step 6で「タプルの型注釈構文（`Tuple2[Int,String]`のような書き方）は実装しない」という意図的なスコープカットを行った際の理由は「タプルリテラル自体が要素の型を完全に決定するため、型注釈を書く動機が薄い」というものだった——これはリテラルを**書く側**（`let`の右辺にタプルリテラルがある場合）には正しいが、Step 11で`parse[T](s) -> Tuple2[T, Error]`のような組み込み関数の戻り値を受け取るユーザー定義関数を書こうとして初めて、「関数の**戻り値型注釈**にはそもそも参照できるリテラルが存在しない」という非対称性に気づいた——`fn f() -> Tuple2[Int, Error] { ... }`は、本体を書く前の時点でまず戻り値の型を宣言する必要があり、迂回のしようがない。

**一般化した教訓**：ある複合型について「型注釈構文は実装しない、リテラルから型推論すれば十分」という判断を下す際は、「その型の値を**生成する**リテラルが存在する文脈」だけでなく「その型を**受け取る/返す**関数シグネチャを書く文脈」でも本当に困らないかを確認すること——後者は往々にして、対象の型を返す**組み込み関数や外部関数**が実装されて初めて具体的な必要性が顕在化する（Step 6時点ではTuple2を返す関数が1つも存在しなかったため、この非対称性が問題にならなかった）。スコープカットの判断自体は妥当でも、「いつまで有効な判断か」の前提条件（今回は「Tupleを返す関数が存在しない間は」）をドキュメント化しておくと、後続のステップで同じ検討をゼロからやり直さずに済む。

### 「片方が無名型なら代入可能」というGoの代入可能性規則は1階層しか見てくれない——ネストした named スライス型を汎用ジェネリクスへ渡すと壊れる（Step 11で発見）

対象言語のList[T]をGoの「名前付きスライス型」（`type ListInt []int64`のような、`SLTYPE`が発行するdefined type）として表現する設計は、Step 7以来「Goの代入可能性規則（片方が無名型なら、同じunderlying typeどうしは変換不要で代入できる）のおかげで、標準ライブラリ関数やGoジェネリクス関数へそのまま渡せる」という前提に支えられてきた（`strings.Split`の戻り値`[]string`を`List[String]`型の変数へそのまま代入できる、等）。この前提は**1階層のスライスまでは正しいが、スライスのスライス（`List[List[T]]`）になった瞬間に崩れる**——`List[List[Int]]`のGo表現は`type ListListInt []ListInt`（要素型が「無名の`[]int64`」ではなく「名前付きの`ListInt`」）になり、これを`func Flatten[T any](xss [][]T) []T`のような素朴な1型引数ジェネリクスへ渡そうとすると、`[]ListInt`と`[][]int64`は別のGo型として扱われ、`go build`の型チェックで初めて拒否される（IRの構文チェックでは検出できない、実地検証必須の典型例）。

原因は、Goの代入可能性規則が「V・Tのどちらかが無名型で、双方のunderlying typeが同一」を要求する際、**この比較は1階層分の型構造にしか適用されない**こと——`[]ListInt`のunderlying typeは`[]ListInt`自身であり（スライス型自体はunderlying typeを持たないので、スライス型のunderlying typeはそのスライス型そのもの）、`[]int64`とは全くの別物として扱われる。「名前付き型の入れ子」が一段深くなるだけで、代入可能性のチェーンが切れてしまう。

対策は、Go 1.18以降のジェネリクス制約が持つ`~T`記法（「underlying typeがTであるどんな型も含む」）を使い、外側のスライス型自体を独立した型パラメータにすること：`func Flatten[S ~[]E, E any](xss []S) []E`。これなら`S`に`ListInt`（`~[]int64`を満たす）を、`E`に`int64`を、それぞれ独立に渡せる——外側の「名前付きスライス型」を型パラメータの主体そのものにすることで、代入可能性の1階層制限を型推論の側でバイパスする。

**一般化した教訓**：対象言語のコレクション型を「Goの名前付きスライス/マップ型」として表現する設計を採用している場合、その**要素型自体が同じ設計のコレクション型である**（ネストしたList/Map等）場面に限って、素朴な1型引数のGoジェネリクスヘルパーが型チェックで失敗する可能性を疑うこと——単一階層のスライス操作（map/filter/reduce等、要素を「不透明な型」としてしか扱わない関数）ではこの問題は起きない（要素型が何であれ、その型をそのままTとして渡すだけで済むため）。問題が起きるのは、ヘルパー自身が「外側のスライスを剥がして中身を取り出す」（`flatten`のように、ジェネリクス関数の型パラメータの数がAmiFLの型のネスト構造の階層数と一致する必要がある）操作の場合に限られる——この違いに気づかず全てのヘルパーを同じパターンで書くと、テストケースがたまたまネストしたコレクションを使わない限り見逃される類のバグになる。

### トップレベル`fn`を値として渡す高階関数はまだ未対応でも、`let`束縛のクロージャーを渡す高階関数はゼロコストで実現できた（Step 11で発見）

Step 5は「トップレベル`fn`を第一級の値として関数へ渡す」（真の高階関数）を意図的に先送りしていたが、Step 11で`map`/`filter`/`reduce`/`sortBy`という「クロージャーを引数に取る組み込み関数」を実装する段になり、**この先送りが実際には全く問題にならない**ことが分かった——理由は、これらの組み込み関数の「シグネチャ」がAmiFLのユーザー向け型注釈構文（`fn(T)->R`という書き方は今も未実装のまま）を一切経由せず、コンパイラ内部のGoコード（sema側のリゾルバー関数）がAST上の引数の型を直接調べて判断するため。`f`の実際の値は常に「`let`で束縛済みのクロージャー変数への参照」（`ast.IdentExpr`）であり、その型（Step5が確立した`"fn(T)->R"`という内部エンコーディング文字列）を`checkExpr`で読み取ってパースするだけで、ユーザー向け構文を1文字も追加せずに「関数を引数に取る関数」が実現できた。

**一般化した教訓**：「ユーザー向け構文のサポートが無い」ことと「その機能が使えない」ことは必ずしもイコールではない——**組み込み関数（コンパイラ自身が「呼び出し可能」だと知っている特別な名前）の引数型は、ユーザーが書ける型注釈の表現力とは独立に、コンパイラの実装言語（今回はGo）側で自由に検査できる**。ユーザー向けの高階関数（`fn compose(f: fn(Int)->Int, g: fn(Int)->Int) -> fn(Int)->Int`のような、ユーザー自身が宣言する関数がクロージャー引数を取る場合）を実現するには依然として`fn(...)->...`型注釈構文の実装が必要だが、「コンパイラの標準ライブラリ関数がクロージャーを取る」というユースケースだけなら、型注釈構文の欠如は障害にならない。将来同様の「まだ表面構文が無い機能」に直面したときは、まず「これは組み込み関数だけで足りる需要か、ユーザー定義の場所でも必要か」を切り分けると、不要な先行実装を避けられる。

### 組み込み関数名を後から予約すると、それ以前に書かれたコードが（テストが通っていたにもかかわらず）静かに壊れうる（Step 11で発見）

Step 11で13.5節の`add`をSetの組み込み関数名として新たに予約したところ、Step 5から存在していた`examples/functions.aml`の`fn add(a: Int, b: Int) -> Int`（ただの足し算関数）と、関連する3つのsemaユニットテストが、`go test`実行時に初めて型エラーで落ちた——`add(3, 4)`という呼び出しが「ユーザー定義のadd関数」ではなく「新しく予約されたSetのadd（第1引数はSetでなければならない）」と解釈されるようになったため。この言語の設計（`print`以来確立している「組み込み関数はどんな呼び出し箇所でも同名のユーザー宣言より優先される」という規則）そのものは正しく機能しており、**バグではなく設計どおりの正しい動作**だったが、影響範囲を事前に予測しきれなかった。

**一般化した教訓**：言語の初期実装段階で「組み込み関数名の完全なリストは仕様で最初から決まっている」としても、**実装をステップごとに小分けして進める以上、ある名前が「まだ予約されていない、ただの識別子」として使えてしまう期間が実在する**——今回のケースでは、`add`はStep 11で初めて組み込みになったが、Step 5〜10のサンプルコードやテストコードはその制約を知らずに書かれていた。新しいステップで組み込み関数名の集合を拡張する際は、**予約する名前の一覧を、既存のexamples/・テストファイル全体に対して機械的に検索してから作業を始める**（このプロジェクトでは実際に`grep`でexamples/と両テストファイルを走査し、3箇所の衝突を発見してリネームで解決した）——`go test`が失敗として教えてくれるとは限らない（型が偶然合致してしまえば、意味が変わったまま静かに成功する可能性もある）ため、テストが通ることに頼らず先回りで確認する価値がある。

### `GTE`/`ADD`等の比較・算術オペランドはGoの型が完全一致していないとコンパイルエラーになる——`for`のループカウンタを流用するときは要注意（Step 12で発見）

Step 7の`emitIndexLoopHeader`が確立した「インデックス用一時変数は素の`^int`で宣言する」という慣習を、Step 12の`take(s, n)`/`skip(s, n)`（`n`はAmiFLの`Int`＝Go`int64`）実装で無自覚に踏襲してしまい、`GTE %doneTmp %iTmp %nVal`が`int`と`int64`の比較になって`go build`で型エラーになりかけた（IRの構文チェックでは検出できない）。原因は`emitIndexLoopHeader`の`^int`が「Goの組み込み`len`が返す素の`int`」に合わせた選択であるのに対し、`take`/`skip`のループカウンタは「AmiFLの`Int`型の引数`n`」と直接比較する必要があり、両者は起源が異なる別のGo型だったこと。**一般化した教訓**：ループカウンタ用の一時変数の型は「何と比較・演算されるか」で決めるべきで、既存の類似パターン（インデックスループ）をコピーする際に「たまたま`int`だったから」で踏襲しないこと——比較・算術命令（`GTE`/`LT`/`ADD`/`SUB`等）はオペランド双方のGo型が完全一致していることを要求する。

### Stream[T]の relay 系操作（`take`/`skip`/`lines`）はamiflrtを介さず、`CLOS`+`DEFER ?close`+`SPAWN`で直接手組みできた——Cascadeの先例がそのまま通用した（Step 12で確認）

`ignored/cascade_implementation_notes.md`が「`DEFER`の綺麗な使い道：パイプラインの各段の関数本体先頭で無条件に`DEFER ?close <出力チャネル>`を発行するだけで、後始末が構文に一切現れない形で保証できる」と書いていたパターンを、AmiFLでもそのまま踏襲できた。AMIVMの`CLOS`はGoのクロージャーリテラルを生成するため、コンパイラが（ユーザーの`ast.ClosureLit`を経由せず）**codegen自身が内部的に合成するクロージャー**をIRとして直接書き出す、という使い方が問題なく成立する——`CLOS`の中身は結局ただのGoコードなので、「ユーザーが書いたクロージャーを翻訳する」用途と「コンパイラが機能を実装するための内部クロージャーをその場で生成する」用途を区別する必要が無い。真の同期を要する`parallel`（N goroutine分のWaitGroupカウント）だけがAMIVMに対応命令が無く、amiflrtへ切り出す必要があった——「単純なCHRECVループ+DEFER close」で足りるか「複数goroutine間の完了カウント同期が要るか」が、amiflrtへ切り出すかどうかの分岐点になった。

### Goのチャネル方向性・代入可能性規則により、名前付き双方向チャネル型をamiflrtの無名の方向付きチャネル引数へそのまま渡せた（Step 12で確認）

`Chan[T]`/`Stream[T]`はそれぞれ独自の`CHTYPE`（名前付き双方向`chan T`）を発行する設計（Set/Mapが別々の`MPTYPE`を発行するStep10の前例を踏襲）だが、`parallel`が使うamiflrtの`ParallelStream[T any](in chan T, workers int) chan T`のように、**パラメータの型を無名の`chan T`（方向指定無し、または`<-chan T`のような方向付き）のまま書くだけで、AmiFL側の名前付きCHTYPE値をそのまま渡せる**——Goの代入可能性規則は「片方が無名型なら同一underlying typeどうしは変換不要」に加えて「双方向チャネル→方向付きチャネルへの暗黙変換」も認めているため。CLAUDE.md「過去に踏まれた地雷」#7（参照型はコピーせずそのまま使い回す）の直接の拡張として、チャネル型もこの原則の対象に含めてよいことが確認できた。

### Goのメソッドを「レシーバを第1引数として受け取る関数」にbindするには、`METHVAL`（メソッド値取得）+`CALL`の2命令構成が必要——AMIVMの`callname`オペランドはドット1個までしか許さないため（Step 13で発見）

`?pkg.Func`という1ドットの`CALL`直呼びは、Goのpackage-level関数にしか使えない——`(t Time) Unix() int64`のようなメソッドをGoのmethod expression構文（`time.Time.Unix`という2ドットの識別子連鎖）で直接呼ぼうとしても、AMIVMの`callname`オペランドカテゴリ自体がドット1個までしか受け付けない（`amivm_spec.md`のオペランドカテゴリ表で確認）。対策は`METHVAL local receiver <method`（`local := receiver.method`という、レシーバーの値からメソッド値を取り出す専用命令）でメソッド値を一度変数へ落としてから、それを`CALL`のcallnameとして使う——`callname`オペランドは`%xxx_123`（ローカル変数）も受け付けるため、この2段構成が素直に成立する。レシーバー自体は`METHVAL`の時点で値へ折り込まれるため、続く`CALL`の引数列には二重に渡さない（`t.Unix()`がGoで引数無しなのと同じ）。

### `Any`型境界は、静的型付け言語では`ASSERT`もreflectも要らないことがある——動機は「呼び出し先の型を静的に知っているかどうか」で決まる（Step 13で発見、Weaveの前例との対比）

Weave（動的型付け）の`gofunc`/`gomethod`は最終的にreflectベースの`CallGoFuncList`へ帰着した——Weaveの変数は常に`^any`であり、生成コードが呼び出し先の具体的なGo型を静的に知る手段が無いため。AmiFLは`extern bind`宣言が引数・戻り値の型（`Any`を含め）を宣言時点で常に確定させる静的型付け言語であり、**`Any`型の値を具体型へ絞り込む（narrowingする）ビルトイン関数を1つも持たない**（`typeName`は動的型名の文字列を返すだけで、値そのものは取り出さない）ため、reflectに頼る動機自体が構造的に存在しなかった。実装はGoの`any`への暗黙のインターフェース変換にそのまま乗るだけ（`goTypeNames["Any"] = "any"`という1行）で完結し、`ASSERT`はStep 13でも一度も出番が無かった。**静的型付け言語でAny/dynamic型を扱う際は、「その値を後で具体型へ戻す操作が言語仕様に存在するか」を最初に確認する価値がある**——存在しなければ、Weave的なreflect機構は過剰設計になりうる。

### Goの無型定数を`any`へboxingする際、Go自身のデフォルト型規則が言語側の意味論と食い違うことがある——`go build`まで通して初めて発覚する典型例（Step 13で発見）

整数リテラル引数を`Any`型（Goの`any`）境界へそのまま渡すと、`genValue`が返す値トークンはGoの**無型定数**（例："5"、型注釈なし）のままになる。Goが無型定数を`interface{}`へboxingする際は**その定数自身のデフォルト型**（整数なら`int`）を採用する——フロントエンド言語側のsemaが「無コンテキストなら`Int64`」と決めていても、生成されたGoコード自体にその決定を伝える型情報は残っていない。結果、`typeName(5)`が期待する`"int64"`ではなく`"int"`を返す、という`go build`＋実行まで通して初めて発覚するバグになった（IRの見た目だけでは分からない、Go言語自体の無型定数の意味論に起因するため）。対策は「Any境界を越える直前に、値トークンが無型リテラルらしい形（数字/`-`始まりで`.`/`e`/`E`を含まない）なら明示キャストを挟む」という値トークンの文字列形状だけを見る軽量な判定で足りた——変数参照は宣言時に型が確定済みのため対策不要、浮動小数点無型定数はGoのデフォルト（`float64`）が元から一致するため同じく対策不要。**「無型リテラルが型不明な境界（`any`・`interface{}`相当）を直接越える」箇所は、ホスト言語自身のデフォルト型規則とフロントエンド言語の意味論が食い違いうる要注意ポイント**として次の実装者への申し送りに値する。

### 複数行にまたがるブロック型宣言（`STTYPE`〜`ENDSTTYPE`等）の内側で、ネストした型解決を行ってはいけない——単一行で完結する型宣言には無い罠（Step 13で発見、Step 6以来の潜在バグ）

`STTYPE`ヘッダー行を書いた直後に、各フィールドの型を解決するループへ入る実装（`STTYPE ^Name` → ループ内で`resolveGoType`→`FIELD`... → `ENDSTTYPE`）は、フィールドの型がプログラム中で初めて登場する複合型（List/Array/Set/Map/Tuple/Chan/Stream）だと、その型解決が**同じ出力バッファへその場で**別の型宣言（`SLTYPE`等）を書き込んでしまい、まだ閉じていない`STTYPE`ブロックの内側に別の宣言が挟まる、という壊れたIRを生成する。単一行で完結する型宣言（`SLTYPE`/`ARTYPE`/`MPTYPE`/`CHTYPE`など、「ネストした型を解決→その1行を書く」で完結するもの）にはこの罠が無い——罠は「ヘッダー行と閉じ行の間に複数行を書く」ブロック形の宣言に固有。対策は「ブロックの中身になる全ての型を先に解決し尽くしてから、ヘッダー行を含めてブロック全体を書き出す」という順序に統一すること。**このバグはStep 6（`struct`導入）から潜在していたが、これまでのexamplesがstruct/enumのフィールドにスカラー/別struct型しか使っていなかったため一度も顕在化しなかった**——Step 13のextern機構が「プログラム中で初めて登場する複合型」を戻り値に使いやすい機能（`Tuple2[Bytes, Error]`等）だったことで、実地検証を通して初めて発覚した。「これまで踏まれていない組み合わせ」は先行実装の実績があっても安心できない、という教訓の具体例。

## 2. 開発プロセスの教訓

`CLAUDE.md`の「開発の進め方」節を参照。「機能単位の縦切り+各ステップでamivm→go buildの実地検証必須」という3言語共通の最重要教訓は、AmiFLでも変わらず適用する。
