# AmiFL言語 詳細仕様書

> 汎用データ処理特化言語AmiFLの詳細仕様書。`ignored/amifl-concept.md`の設計方針に基づく。
>
> **本書は実装完了後（全15ステップ完了、`CLAUDE.md`参照）に、実装（`internal/lexer` `internal/parser` `internal/sema` `internal/codegen` `amiflrt`）と1行ずつ突き合わせて全面的に書き直したものである。** 旧版には「仕様として書いたが実装されなかった」項目（`return`/`Never`型、16進/8進/2進数値リテラル、桁区切り`_`、パイプ右辺インラインクロージャー、`switch`の`is Type`/`in [...]`パターン、`map`/`filter`/`reduce`のStream/Chan対応等）が複数残っていた。本書はそれらを全て**実装の実際の挙動**に合わせて訂正し、未実装の項目は「未実装」として明記する（17節に一覧化）。実装とAPIが今後変わった場合は、まず本書の記述を疑い、確定させてからコードを直すこと（`CLAUDE.md`の開発方針どおり）。この刷新の直後に`fn main(args: List[String]) -> Int`（14節、ex1）・`Range`（3.1/7.3節、ex2）・`Func`型の表面構文と高階関数の一般化（8.3節、ex3）・パイプ右辺インラインクロージャー（9節、ex4）・パッケージ間`struct`/`enum`参照（12.2節、ex5）・`print`の`Any`一般化と`eprint`/`format`/`formatWith`/`exit`（13.1節、ex6）・16進/8進/2進数値リテラルと桁区切り`_`（3.1節、ex7）・複数行カンマ区切りリテラル/`|>`チェーンの行またぎ継続（5節、ex8）・`&&`/`||`の短絡評価（6節、ex9）・構造体フィールド代入`p.x = 5`（2.2/3.2節、ex10）・`return`/`Never`型（2.2/5節、ex11）・`write()`が`Bytes`だけでなく`String`も直接受け付けるようにする一般化（13.10節、ex12）・`let (a, b, ...) = value`によるTuple2〜Tuple8の一括分解代入（4節、ex13）が実装され、本書はその都度追随済み——17.3節の「将来的な拡張の余地」からは既に除外してある。

**ライセンス**: MIT

---

## 0. 前提

- **静的型付け**。値の種類は常にコンパイル時に確定し、実行時の動的ディスパッチは無い。
- 位置づけ：「AWK/Perlの系譜だが、Perl的な乱雑さを避けた汎用データ処理特化言語」（`ignored/amifl-concept.md` 1節）。
- ファイル拡張子は`.aml`。
- 実装言語はGo。コンパイルパイプラインは3工程：

  ```
  AmiFLソース (.aml)
    ↓ 本リポジトリ（internal/lexer → internal/parser → internal/sema → internal/codegen）
  AMIVM-IR (.ir)
    ↓ amivm（外部CLIツール。別リポジトリ、go installでPATHへ導入）
  Goコード (.go)
    ↓ go build（amifl buildが内部で実行）
  実行ファイル
  ```

  **AmiFLの責務は「AmiFLソース → AMIVM-IR」の生成まで。** AMIVM-IRの意味論的な正しさ（型の整合性等）はamivm側が`go/types`に委ねて検証するため、AmiFL自身の意味検査（`internal/sema`）がその防波堤を一手に引き受ける。

---

## 1. 設計思想（一貫して現れるモチーフ）

`ignored/amifl-concept.md`の「Perl的な乱雑化を避ける」という一番の動機を、以下7つの原則として具体化する。実装全体を通じて、迷ったときはこの7原則へ立ち返って判断してきた。

1. **式指向・文の排除**：プログラムは式の並びのみ。`if`/`switch`/`for`/`while`/`fn`/`let`/`const`は全て「値を持つ式」。ブロックの値は最後の式。最後以外の式は`Unit`型でなければならない（戻り値の握りつぶし忘れ防止。捨てたいときは明示的に`_ = 式`）。詳細は5節。
2. **暗黙変換の排除**：サイズ・符号違いの数値型は相互に暗黙変換しない。異なる型どうしの`==`もコンパイルエラー。明示変換は`cast[T](v)`のみ。
3. **1つの仕組みで足りるものを2つ用意しない**：多次元配列に専用構文を作らず`Array[T;N]`のネストの糖衣にする、添字も`x[i,j]`のような別構文を導入せず`x[i][j]`の連鎖にする、チャネル送受信も専用記号（`<-`）を導入せず組み込み関数`send`/`recv`にする、パターンマッチも専用`match`構文を作らず`switch`の拡張にする。
4. **ユーザー拡張ポイントを絞る**：関数のオーバーロード無し、ユーザー定義capability（多相の型クラス的機構）拡張無し、可変長引数無し、カリー化無し、`struct`/`enum`にメソッドを持たせない。多相が必要な場面はコンパイラ組み込みの多相関数（`len`/`slice`等）として個別対応し、一般的な拡張機構は提供しない。
5. **グローバル可変状態の排除**：トップレベルの`let`（グローバル変数）を禁止。状態共有が必要なら`Chan[T]`を介した明示的な受け渡しを使う。
6. **戻り値は常に単数、複数値はタプルで包む**：「値+エラー」パターンは常に`Tuple2[T, Error]`という1つの形に統一する。これにより`?`演算子やパイプ演算子が前提とする「1つの値が流れる」モデルを保てる。
7. **明示性 > 簡潔さ**：可変長引数を許さず、複数値は`List`/`Tuple`にまとめて渡す。名前付き引数・デフォルト値付き引数は提供しない（8.4節）。関数・クロージャー引数は再代入不可（`let`だけが再代入可能）という判断もこの原則に基づく。

---

## 2. 型システム

### 2.1 スカラー型

- 符号付き整数：`Int8` `Int16` `Int32` `Int64`（`Int`は`Int64`のエイリアス、既定の整数型）
- 符号なし整数：`UInt8` `UInt16` `UInt32` `UInt64`（`UInt`は`UInt64`のエイリアス）
- 慣用名エイリアス：`Byte`(=`UInt8`)、`Rune`(=`Int32`、Unicodeコードポイント用)
- 浮動小数点：`Float32` `Float64`（`Float`は`Float64`のエイリアス、既定）
- `Bool`、`String`（UTF-8、イミュータブル）
- サイズ・符号の異なる数値型どうしは暗黙変換しない（`Int32`の値をそのまま`Int64`の変数へ代入することもできない）。明示変換は`cast[T](v)`（13.3節）。`Int`と`Int64`、`Byte`と`UInt8`のようなエイリアス対は同一の型として扱われる（`canonicalType`が正規名へ解決してから以降の全処理が動く）。

**リテラルは「無型」として扱われ、使われる文脈（`let`の型注釈・二項演算の相手側の型・関数引数の宣言型等）に応じて具体的な型が決まる**（Goの無型定数に近い考え方）。整数リテラルは期待型が整数系ならその型として妥当性（オーバーフロー）を検査し、期待型が浮動小数点系ならそのまま採用、期待型が無ければ`Int64`が既定。浮動小数点リテラルは整数系型へは決して適応しない。二項演算`a op b`では「具体型が既に決まっている方（変数・`const`参照等）を先に解決し、リテラルの方をその型に合わせる」——`count + 1`と`1 + count`のどちらでも`count`の実際の型（例えば`Int8`）が優先される、非可換なバグを避けるための順序である。

**数値リテラルの構文**：10進の整数（`42`）・浮動小数点（`3.14`/`1.23e4`、`e`/`E`指数表記込み）に加え、16進（`0x1F`）・8進（`0o17`）・2進（`0b101`）表記、桁区切り`_`（`1_000_000`）をサポートする（ex7）。詳細は3.1節を参照。

### 2.2 コレクション・複合型

- **`Tuple2[A,B]`〜`Tuple8[...]`**：固定長・異種混合。要素数2〜8。リテラルは`(v1, v2, ...)`。1要素だけの場合`(x)`は単なる括弧付き式（グループ化）になってしまうため、1要素タプルは`(x,)`と末尾カンマで書く。フィールドアクセスは`.0` `.1` ...の糖衣（実行時表現は`F0`/`F1`/...という名前のフィールドを持つ合成`struct`、下記参照）。メソッドは無く、フィールド代入も無い（読み取り専用）。要素の型としてタプル自身・`Func`型は使えない（ネストしたタプル・関数を要素に持つタプルは書けない、意図的なスコープ限定）。`Tuple2[T1,T2]`〜`Tuple8[...]`という型注釈構文は書ける（関数の引数・戻り値等で使う。タプルリテラル自身は要素の型を完全に決定するため、`let`の初期化式がタプルリテラルの場合は型注釈を省略してよい——むしろ`let`の値としては注釈を書けない）。**`==`/`!=`比較が可能**（全要素が比較可能な限り、Goのネイティブ構造体比較へ素直に落ちる）。同じ要素型の並びを持つタプルは、プログラム中どこで書いても常に同じ内部Go型（型ごとに1つ発行、使い回す）になる。
- **`Set[T]`**：重複無し。`T`は比較可能な型（数値・文字列・真偽値・タプル。**`struct`/`enum`要素は使えない**）に限定。リテラル`{1, 2, 3}`。要素の順序は不定（Goのmapイテレーション順序をそのまま使う。決定的な順序が欲しい場合は`toList(_) |> sort`と組み合わせる、17節参照）。実行時表現は`Map[T, Bool]`と同じ（`map[T]bool`）だが、AmiFLの型としては`Map[T,Bool]`と最後まで別物として扱われる（相互に代入不可）。
- **`Array[T; N]`**（固定長・コンパイル時サイズ）と**`List[T]`**（可変長）：明確に2つの型として分離する。`Array`はGoのネイティブ固定長配列（値型）、`List`はGoのスライス（参照型）としてコンパイルされる。既定のリテラル`[1,2,3]`は`List[T]`。型注釈で明示したときのみ`Array[T;N]`になる。要素は改行を挟んで複数行に書け、末尾カンマも付けられる（`[1,2,3,]`は合法、ex8。5節参照）。
  - **多次元配列**`Array[T; N1, N2, ...]`は`Array[Array[...Array[T;Nk]...]; N2]; N1]`のネストの糖衣構文にすぎない（パーサーが構文解析時点で1次元ずつの型ノード列へ展開する）。添字も`x[i][j]`の連鎖で足り、`x[i,j]`という別構文は導入しない。
  - コレクションリテラルの型注釈は、多次元配列などネストの各階層へ再帰的に伝播する（`Array[Int;2,2]`への代入で`[[1,2],[3,4]]`と書くと内側も自動的に`Array[Int;2]`と解釈される）。無注釈時は各階層独立に既定（`List`）が適用される。
  - 配列サイズ`N`は整数リテラル、またはリテラルへ辿り着く`const`参照のみ（演算を含む`const`式はサイズに使えない、意図的なスコープ限定）。
- **`Bytes`**：可変長バイト列。**実装上は`List[UInt8]`への完全なエイリアス**（`canonicalType`が識別子`Bytes`を`List[UInt8]`へ直接読み替える）。`len`/`slice`/`concat`/`x[i]`/`x[a:b]`等、`List[T]`が持つ全機能をそのまま使える。副作用として、型エラーメッセージには`Bytes`ではなく`List(UInt8)`と表示される場合がある。
- **`Map[K,V]`**：連想配列（Goのネイティブmap）。`K`は`Set[T]`の`T`と同じ制限（数値・文字列・真偽値・タプルのみ、`struct`/`enum`不可）。`V`には制限が無く、List/Array/Set/Map/Chan/Stream等のコレクション型もそのまま持てる（ネストしたMapも書ける）。
- **`struct`**：ユーザー定義。フィールドのみでメソッドは持たない。フィールドは`p.x = 5`で代入できる（ex10、3.2節参照）——値を丸ごと作り直す（`Point{x: p.x + 1, y: p.y}`のように）か、フィールドだけを書き換えるかは自由に選べる。タプルのフィールド（`.0`等）は代入できない点と対照的（タプルは不変値として扱う、上記の`Tuple2`〜`Tuple8`の項参照）。
  ```amifl
  struct Point { x: Float, y: Float }
  ```
  全フィールドがスカラー・別の`struct`・タプルの場合、Goのネイティブ構造体比較がそのまま使えるため`==`/`!=`比較が可能。
- **`enum`**（タグ付きバリアント）：複数の「形の異なる場合分け」を1つの型として表現する。各バリアントは0個以上の名前付きフィールドを持てる。
  ```amifl
  enum Status {
      Ok
      Retry(delay: Int)
      Failed(reason: String)
  }
  let s2: Status = Status.Retry(delay: 5)
  ```
  値生成は`型名.バリアント名(...)`（フィールドが無いバリアントは括弧無し、`Status.Ok`）という、リテラルではない通常の式。フィールドは名前付き引数の形（`delay: 5`）で渡す。**バリアント判定・フィールド取り出しは`switch`のパターンマッチでのみ行う**（10節）。バリアント集合はコンパイル時に閉じている（実行時追加は不可）。実行時表現は「`Tag`（バリアントの宣言順位置を表す整数）と全バリアントの全フィールドを1つに統合した`struct`」（`<バリアント名>_<フィールド名>`という合成フィールド名で衝突を避ける）——アクセスしていないバリアントのフィールドはGoのゼロ値のまま放置される（無害、`switch`が保証する範囲でしか読まれない）。**`==`/`!=`比較は明示的に禁止**（唯一の正当なアクセス手段は`switch`であり、直接比較を許すと異なるタグどうしのゴミフィールド比較という実装詳細が漏れるため）。
- **`Stream[T]`**（遅延・ファイル/標準入出力・並列処理向け）／**`Chan[T]`**（並列処理用キュー）：実行時表現はどちらもGoのネイティブchannelで同一だが、**AmiFLの型としては最後まで別物**として扱われ、相互の暗黙変換は無い（明示変換関数も未実装、17節）。`send`/`recv`/`chan[T]`は`Chan[T]`専用、`take`/`skip`/`collect`/`parallel`/`lines`/`for`のBody形反復は`Stream[T]`専用（11節・13.8節）。
- **`Range`**（`a..b`/`a..=b`で生成する数値範囲、詳細は3.1節）：`From`/`To`とも`Int64`固定。実行時表現は`{From, To int64}`という1つの合成`struct`（`STTYPE`はプログラム全体で1回だけ発行、常に半開区間へ正規化——`..=`のTo側は構築時に+1される）。`Func`型と同様、**ユーザーが書ける型注釈構文は無い**（値の生成・`let`束縛・`for`消費はできるが、`Range`という名前を`: 型`として書く場所は無い）ため、`List[Range]`のような合成もできない。`Set[T]`/`Map[K,_]`の要素・キーにもなれない（比較可能型の一覧に含まれない）。
- **`Any`**：2つの異なる文脈で登場する型注釈。(1) `extern`境界（15節）の`bind`引数・戻り値——Goとの境界を越える時点で具体型が消える、真に動的な型。(2) `print`/`eprint`/`format`/`formatWith`/`typeName`（13.1/13.2節）が引数に使う「無制約な多相引数」——2.4節のcapability多相が閉じた型集合（数値系・Lenable等）に限定されるのに対し、こちらはどんな具体型でも受け付ける全称的な多相だが、各呼び出し箇所の具体型へコンパイル時にモノモーフィックに解決される点はcapability多相と同じ設計思想の延長にある（実行時ディスパッチは無い）。いずれの用法も実行時表現はGoの`any`そのもの（Goの暗黙のインターフェース変換にそのまま乗る）で、AmiFLには`Any`値を具体型へ絞り込む（narrowingする）手段が無く、`typeName`で動的型名を覗く以外の使い道が無い——原則2（暗黙変換の排除）どおり、`Any`型の値を具体型が期待される場所へ渡すことは拒否される。
- **`Error`**：Goの`error`インターフェースとして表現。`isError(v)`（13.2節）が非nilかどうかを判定する。**AmiFLには`nil`リテラル（あるいはそれに相当する構文）が存在しない**——ユーザーコードは自分で「新しいエラー無し値」をゼロから作ることはできず、既存の（`parse`等の組み込み関数が返した）`Tuple2[T,Error]`のエラー部分を素通しする、という使い方に限られる。
- **`Unit`**（値が無いことを表す、サイズ0）：ブロックの途中に置く式は全て`Unit`型でなければならない（5節）。
- **`File`**（不透明なファイルハンドル）：実行時表現は`*amiflrt.FileHandle`という、AmiFLからはフィールドの見えないGoの構造体ポインタ。生成・操作は13.10節の組み込み関数のみを通す。
- **`Func`型**：`fn(T1, T2, ...) -> R`という記法で表現される、関数値の型（ex3で型注釈構文を獲得済み——詳細は8.3節）。`let`・`fn`/クロージャーの引数・戻り値・`struct`のフィールドなど、型注釈が書ける場所ならどこでも使える。クロージャーリテラル（`fn(x: Int) -> Int { ... }`）自身は依然として**`let`の直接の初期化式としてのみ書ける**（呼び出し引数へインラインで書く、別の式へ埋め込む、といった用途は未実装のまま——一度`let`で名前へ束縛してから、その名前を値として渡す）。ただし`let`の値として書く際は、クロージャーの自己推論した型と一致する限り、明示的な`fn(...)->R`型注釈を添えてもよい。値としての`Func`は`let`束縛のクロージャーに限らず、**トップレベル`fn`をその名前で直接値として参照する**こともできる（`let f = add`、`apply(add, 5)`）——これによりユーザー定義の`fn apply(f: fn(Int)->Int, x: Int) -> Int { f(x) }`のような、真の高階関数が書ける。関数型どうしの`==`比較は不可。
- **`Never`型**：`return`/`break`/`continue`（5/7節、ex11）が持つ、コンパイラ内部限定の型——ユーザーが型注釈として書くことはできない。これら3つはどこに現れても制御が発散する（`return`は関数/クロージャーの外へ、`break`/`continue`はループの外へ）ため実際には値を作らないが、`Never`はどんな期待型とも自動的に適合する（型不一致検査を素通りする）ことで、`if done { return 5 } else { 10 }`のような、`return`/`break`/`continue`を他の分岐と混在させたif/switchの分岐を型検査できるようにする。この`Never`の値は表面上ユーザーには一切見えない（エラーメッセージにも決して現れない）——AmiFLの型注釈文法自体に`Never`という書き方は存在しない。

### 2.3 型の比較可能性のまとめ

| 型 | `==`/`!=` |
|---|---|
| スカラー（同じ型どうし） | 可 |
| `Tuple2`〜`Tuple8` | 可（全要素が比較可能な限り） |
| `struct` | 可（全フィールドが比較可能な限り） |
| `enum` | **不可**（`switch`のみが正当なアクセス手段） |
| `Func`型 | 不可 |
| `List`/`Map`/`Set` | **不可**（semaが明示的に拒否する。Goの`[]T`/`map[K]V`はネイティブに非比較可能——`nil`との比較にしか使えず、AmiFLには`nil`リテラルが無いため実質使い道が無い） |
| `Array`/`Chan`/`Stream` | 可（`Array`はGoのネイティブ固定長配列の要素ごとの値比較、`Chan`/`Stream`はGoのネイティブチャネルの参照〈同一チャネルかどうか〉比較——値の中身は比較しない点に注意） |
| `extern`型（15節） | Go側の`==`がコンパイルできる限り許可されてしまう（AmiFL側semaはGo側の比較可能性を静的に知りえないため、既知の限界） |

### 2.4 組み込み多相性（「能力（Capability）」ベース）

`len`・`slice`・`contains`のような組み込み関数の多態は、ユーザーに継承・メソッドとして公開せず、**コンパイラ内部限定の非公開多相タグで実現する**。以下の表は実装（`internal/sema`の各`resolveXxx`関数）が実際にどの型を受け付けているかをまとめたものであり、**`capability Lenable { ... }`のようなブロックはAmiFLの構文として一切存在しない**（旧版の本節はこれをあたかも書けるAmiFL構文であるかのように示していたが、誤りだった——`capability`はキーワードとしてすら実装されていない、純粋にドキュメント上の分類名である）。

| 能力（分類名） | 対応する型 | 使用箇所 |
|---|---|---|
| Lenable | `String`, `List[T]`, `Array[T;N]`, `Map[K,V]`, `Set[T]`, `Chan[T]`（`Bytes`は`List[UInt8]`経由で対象） | `len` |
| Sliceable | `String`, `List[T]`, `Array[T;N]`, `Stream[T]` | `slice`、`x[a:b]`糖衣（`Stream`は`x[a:b]`糖衣の対象外、`slice()`関数形のみ） |
| Containable | `String`, `List[T]`, `Array[T;N]`, `Map[K,V]`（キー）, `Set[T]` | `contains` |
| Ordered | `Int`系, `UInt`系, `Float`系, `String` | 比較演算子`< <= > >=`、`sort`/`sortBy`のキー型 |
| Numeric | `Int`系, `UInt`系, `Float`系 | 算術演算子、`cast`/`parse`の対象型、`min`/`max`/`abs`/`clamp` |
| Concatenable | `String`（`+`演算子のみ）／`String`, `List[T]`（`Bytes`含む、`concat()`関数のみ） | 6節参照。**`+`演算子は`String`専用**——`List`/`Array`の連結は`+`ではなく`concat()`（13.4節）を使う |
| EqualityComparable（List/Array要素用） | 数値・文字列・真偽値・タプル・`struct`（`enum`は除く） | `contains`/`index`/`unique`のList/Array要素の比較 |
| ComparableKey（Set要素・Mapキー用） | 数値・文字列・真偽値・タプルのみ（`struct`も不可） | `Set[T]`の`T`、`Map[K,V]`の`K` |

多相の解決は各呼び出し箇所ごとに引数の静的型から一意に確定する、実行時ディスパッチ皆無の完全なモノモーフィック多相である。ユーザー定義capability拡張・同名関数の型ごとの乱立（`str_len`のような別名）は禁止。

---

## 3. リテラルと糖衣構文

### 3.1 リテラル一覧

```amifl
42          3.14        1.23e4
0xFF        0o17        0b101      // 16進・8進・2進の整数リテラル（ex7）
1_000_000   1_000.5                // 桁区切り`_`（ex7。整数・浮動小数点どちらにも書ける）
true        false
"hello\n"
[1, 2, 3]                 // List[Int]（既定。型注釈でArray[T;N]にできる。末尾カンマ可）
{1, 2, 3}                 // Set[Int]（型不明な空`{}`は型注釈必須）
{"a": 1, "b": 2}          // Map[String, Int]
(1, "a", true)            // Tuple3[Int, String, Bool]
(1,)                       // Tuple1相当は無いため、要素数1は末尾カンマで明示（Tuple2以上のみ実在、単独の(x)は単なる括弧付き式）
Point{x: 1, y: 2}         // struct リテラル
0..10       0..=10        // Range（半開区間／閉区間、ex2で実装。7.3節参照）
```

`enum`の値（2.2節）はリテラルではなく`型名.バリアント名(...)`という通常の式で生成する。

**文字列リテラルがサポートするエスケープは`\n` `\t` `\\` `\"`の4種のみ**（Unicodeエスケープ`\u{...}`等は無い）。文字列は改行を挟んで書けない（複数行文字列リテラルは無い）。

**整数リテラルは10進（`0`単体、または`1`-`9`に`0`-`9`が続く形——先頭ゼロ不可）・16進（`0x`/`0X`）・8進（`0o`/`0O`）・2進（`0b`/`0B`）のいずれかで書ける（ex7）**。桁区切りの`_`は基数プレフィックスの直後、または数字と数字の間にのみ置ける（先頭・末尾・`_`の連続、およびプレフィックス無しの`0`単体への付加は不可）——`amivm`自身の整数リテラル文法（ignored/amivm/amivm_spec.md 6節）とそのまま一致し、コンパイラは字句を変換せずamivmへそのまま渡す。桁区切り`_`は浮動小数点リテラル（整数部・小数部・指数部いずれも）にも書けるが、こちらは`amivm`側へ渡す前に常に10進の数値へ再構成される（16進浮動小数点表記は無い）。**先頭ゼロ付きの10進表記（`017`等）はコンパイルエラー**——`0`から始まる複数桁の並びは基数プレフィックスとして解釈されるべきで、桁区切りの`_`が無くても`0o`のように書き直すか、単に先頭の`0`を落とすことが求められる。負の数値リテラルは通常どおりいずれの基数にも前置できる（`-0x1A`等）。

**`a..b`（半開区間）／`a..=b`（閉区間）はRange値を生成する式**（優先順位は`|>`のすぐ内側、6節の優先順位表参照）。`From`/`To`は共に`Int64`固定（`Int8`等の他の整数幅・`Float`は不可、リテラルは通常どおり無型としてInt64へ既定適応する）。値としては`let r = 0..10`のように`let`で束縛でき（型注釈は書けない——後述）、消費できるのは`for x in range { ... }`／`for x in range yield expr`（7.3節）だけ——`len`/`contains`等13.4節の組み込み関数、`.From`/`.To`のようなフィールドアクセス構文は無い。`From >= To`（半開）・`From > To`（閉）はいずれもコンパイルエラーではなく、単に0回反復の空レンジになる（降順・ステップ指定のレンジは無い）。**`Range`という型名を`let`/`fn`引数/`struct`フィールド等の型注釈として書くことはできない**（`Func`型と同じ意図的なスコープ限定——値は生成・`let`束縛・`for`消費はできるが、注釈構文が無いため`List[Range]`のような合成もできない）。

### 3.2 添字・スライス糖衣

| 記法 | 展開先（意味的な対応。実際はASTの専用ノードへ直接コンパイルされ、`at`/`setAt`/`slice`という名前の関数呼び出しを実際に生成するわけではない——13.4節参照） |
|---|---|
| `x[i]` | `at(x, i)`相当 |
| `x[i] = v` | `setAt(x, i, v)`相当（`List`のみ。固定長`Array`は`x[i]=v`のみが可能で、`setAt()`関数形は使えない） |
| `x[a:b]` | `slice(x, a, b)`相当 |
| `x[a:]` | `slice(x, a, _)`相当 |
| `x[:b]` | `slice(x, _, b)`相当 |
| `x[:]` | `slice(x, _, _)`相当 |

`x[i]`/`x[a:b]`はList・Array・Stringが対象（Streamはこの糖衣の対象外——`slice()`という名前付き関数形のみ使える、13.4節参照）。`x[i][j] = v`のような多段代入は、内側から「読み出し→変更→書き戻し」を再帰的に行うことで正しく実装されている（Listは参照型、Arrayは値型という違いを気にせず動く）。

タプルフィールドアクセス`t.0`, `t.1`, ...（Tuple2〜8なので`.0`〜`.7`まで）も糖衣構文で、structのフィールドアクセス`p.x`と全く同じ内部ノードを共有する（意味解決の時点でどちらかが確定する）。

**`struct`フィールドへの代入は`p.x = 5`で書ける**（ex10。タプルの`.0`等は代入不可——2.2節参照）。`x[i]=v`と同じ「読み出し→変更→書き戻し」の仕組みを共有しており、`Index`（`[i]`）と`Field`（`.field`）を自由に混在させた多段代入も書ける：`p.points[i].y = v`・`xs[i].total = v`のように。

### 3.3 `?`演算子（エラー短絡）

「戻り値が`Tuple2[T, Error]`である呼び出し式」の直後にのみ書ける後置演算子。エラーが非`nil`ならその場で（自分を囲む関数から）早期脱出し、`Error`部分を戻り値として抜ける。エラーが`nil`ならペイロード（`Tuple2`の1番目の要素）が式全体の値になる。**自分を囲む関数の戻り値が`Tuple2[U, Error]`（または`Error`単体）の場合にのみ**使用可（コンパイル時に検査される）。

```amifl
fn parseSumTry(a: String, b: String) -> Tuple2[Int, Error] {
    let x = parse[Int](a)?        // aの変換に失敗したら、ここでTuple2[Int,Error]の
                                    // エラー側をそのまま持って早期脱出する
    let py = parse[Int](b)        // bの変換結果はTuple2[Int,Error]のまま保持しておく
    (x + py.0, py.1)               // 成功時はpyが持つ既存のError値（nil）をそのまま使い回す——
                                    // AmiFLには新しいError値をゼロから作る手段が無いため
}
```

`?`は`return`（5節、ex11）の一般的な早期脱出のうち、`Tuple2[U,Error]`/`Error`の伝播という最も頻出するパターンだけを1トークンに圧縮した専用の糖衣——ペイロードのFGET・ゼロ値構築・Error側のFSET・条件付きRETをすべて肩代わりする。`return`があれば同じ処理は`if isError(e) { return (zeroValue, e) }`のように手で書けるが、ペイロード型のゼロ値を自分で組み立てる手間が生じるため、両者は互いを置き換えるものではなく併存する（原則3「1つの仕組みで足りるものを2つ用意しない」に照らしても、`?`は汎用`return`の特殊化であって別の仕組みではない）。「成功時のError値」は常にこのように、どこかの組み込み呼び出しが既に返した`Error`値（`nil`）を素通しすることでしか手に入らない。

---

## 4. 定数と変数

```amifl
const Pi: Float = 3.14159265
let count: Int = 0
```

- **`let`**：実行時変数、初期化式は任意の式、**再代入可**、**トップレベル宣言は禁止**（グローバル可変状態を避けるため、原則5）。同じ関数・クロージャー内でのシャドーイング（同名の`let`を内側のブロックで再宣言する）が可能——内部的には一意な名前へ機械的にリネームされる。
- **`const`**：コンパイル時にのみ存在する値で**実行時ストレージを一切持たない**。初期化式は「リテラル・他の`const`参照・タプル/structリテラル・それらの演算」のみ（宣言順のみ依存、後方参照不可）。トップレベル宣言可。再代入不可。**参照箇所ごとに初期化式がそのまま再生成される**（一度だけ計算して結果をキャッシュするわけではない——コンパイル時定数畳み込みは行わず、Go自身の算術演算意味論をそのまま信頼する設計。副作用の無い式のみが許可されるため実害は無い）。
- 関数・クロージャー引数、`for`の束縛変数は**再代入不可**（原則7、明示性を優先。書き換えたい場合は`let x2 = x`で明示的にコピーする）。
- Unit型の式を`let`/`const`の初期化式にすることは明示的に禁止されている（`let x = print("a")`のようなコードは、`print`呼び出し自体・別の`let`/`const`/代入式のいずれも拒否される）。
- `let x = (let y = 5)`のような、Unit型の式を別の式へ埋め込むパターンもsemaが型不一致として拒否する。
- 同様に、`let`/代入の**直接の値**が`return`/`break`/`continue`（Never型、5節、ex11）であることも拒否される（`let x = return 5`——制御が発散した時点でxへ束縛する値が無い）。`if`/`switch`の分岐の中で`return`/`break`/`continue`を使うのは問題ない（`let x = if done { return 5 } else { 10 }`）——直接の値としてだけ拒否される。
- **`let (a, b, ...) = value`**でTuple2〜Tuple8の値を1文で複数の束縛へ分解できる（ex13）。名前の個数は`value`の実際のタプルの要素数と厳密に一致していなければならない。各名前は独立した通常の`let`と同じ規則（再代入可・シャドーイング可）に従う。名前ごとの型注釈は書けない——タプル自身の型が全要素の型を既に決定しているため。`_`をある位置に書くとその要素を明示的に捨てられる（`let (value, _) = parse[Int](s)`）——`value`は束縛されず、コード生成もその位置に一切命令を出さない。`value`は常に1回だけ評価される（名前の数だけ再評価されることはない）。

```amifl
let (value, err) = parse[Int]("42")
if isError(err) {
    // ...
}
```

---

## 5. 式指向設計の原則

- AmiFLに文(statement)という独立した文法カテゴリは無く、プログラムは式の並びだけでできている。
- `{ ... }`ブロックは式であり、値は最後の式の評価結果になる。改行が式の区切りとして意味を持つ（セミコロンによる区切りや自動セミコロン挿入は無い——1行に複数式を書く構文は無い）。**この「改行が区切りになる」規則はブロック直下でのみ働く。** 一方、`(...)`（呼び出し引数・タプルリテラル・グルーピング）・`[...]`（リスト/配列リテラル・添字・スライス境界）・`{...}`（Set/Map/structリテラル本体、ブロックそのものとは別の`{`）といった、自分自身の閉じトークンで曖昧性なく区切られるカンマ区切りリストの内部では、要素ごとに改行を挟んで複数行に書ける（末尾カンマも許可——`[\n    1,\n    2,\n]`のように書ける）。`|>`チェーンも、次の行の先頭が`|>`であれば前の行から継続する（`data\n    |> f\n    |> g`）——`|>`は他のどんな式の先頭にもなり得ないため、この形は常に「継続」以外の解釈を持たない。**それ以外の式の途中の改行（二項演算子の左右・`if`/`while`条件式など、閉じトークンを持たない位置）は依然として構文エラーになる**——長い式を書く場合は`let`で途中結果に名前を付けて分割する。
- ブロック内の最後以外の式は、その型が`Unit`でなければならない（原則1）。捨てたいときは`_ = 式`と明示する。**`_ = expr`はGoの未使用変数対策のための構文ではなく、純粋にAmiFL自身の型規律のためだけの構文**（対象言語Goコンパイラの都合ではない）。副作用の無い値（リテラル・変数読み取り）を捨てる場合、コード生成は何もIRを出さない。
- `if`/`switch`/`for`（`yield`付き）/`fn`/`let`/`const`は全て式であり、値を持つ。
- **`return`/`return expr`は自分を囲む関数/クロージャーからの早期脱出**（ex11）。値を省略した`return`は、囲む関数/クロージャーの戻り値型が`Unit`のときだけ書ける。書ける場所は`let`/`const`/`_ = ...`と同じ「文の位置」に限られる——ブロック直下の各文（末尾の式も含む）としてのみ書け、呼び出し引数・二項演算子の被演算子のようなより大きな式の内部に埋め込むことはできない（`f(return 5)`・`5 + return 1`はいずれも構文エラー）。これはAMIVM-IRの制約に由来する構造的な制限であり、実用上の妨げにはならない——`if done { return 5 } else { 10 }`のように、ブロックの末尾（if/switchの分岐の末尾を含む）としてなら自由に書ける。`?`演算子（3.3節）は、`Tuple2[U,Error]`/`Error`の伝播という頻出パターンに特化した`return`の糖衣。
- `break`/`continue`（7節）も同様にブロックの末尾として使える（`if done { break } else { 5 }`）——`return`/`break`/`continue`はいずれもコンパイラ内部の`Never`型（2.2節）に解決され、周囲のif/switchの分岐がどんな型であってもそれと矛盾しない。

```amifl
let grade = if score >= 90 { "A" } elif score >= 70 { "B" } else { "C" }
```

---

## 6. 演算子

- **算術**：`+ - * / %`（`Numeric`に多相。左右の型は完全一致必須）。固定長整数のオーバーフローはラップアラウンド（実行時チェック無し）。ゼロ除算は`Int`系でパニック、`Float`系はInf/NaN。単項マイナス`-`もサポートする（リテラル直下では符号を畳み込むだけでAMIVM命令を消費しない。それ以外の式では`0 - x`として合成される。`let x: Int8 = -128`のような、符号付き型の最小値のリテラルも正しく通る）。
  - **`+`は`String`に対してのみConcatenable（文字列結合）として働く。`List`/`Array`/`Bytes`の連結は`+`ではサポートされておらず、`concat()`（13.4節）を使う必要がある。**
- **比較**：`== != < <= > >=`。`==`/`!=`は同じ型どうしのみ、かつ2.3節の比較可能性表に従う。`< <= > >=`は`Ordered`（数値系・`String`）のみ。
- **論理**：`&& || !`（`Bool`専用、truthy/falsy変換無し）。**`&&`/`||`は短絡評価である**（ex9）——`a && b`は`a`が`false`なら`b`を評価せずに`false`を返し、`a || b`は`a`が`true`なら`b`を評価せずに`true`を返す。`j < n && xs[j] == v`のような、右オペランドに境界外アクセスのリスクがある式（`j==n`のとき`xs[j]`が範囲外になる）や副作用（関数呼び出し）を安全に書ける。
- **ビット**：`& | ^ &^ ~`（整数型専用）。
- **シフト**：`<< >>`。
- **範囲**：`.. ..=`（`Range`値を生成、3.1節参照。`|>`の1段内側、他の全演算子より低い）。
- **パイプ**：`|>`（優先順位は最低）。9節参照。

優先順位（高→低）：`() . [] 関数呼び出し 後置?` → 単項`! - ~` → `* / % << >> & &^` → `+ - | ^` → `< <= > >=` → `== !=` → `&&` → `||` → `.. ..=` → `|>`

---

## 7. 制御構文（すべて式）

### 7.1 `if`/`elif`/`else`

`else`省略時は`Unit`型限定。`else`ありなら全分岐が同じ型でなければならない。`elif`/`else`は**直前の`}`と同じ行に**書く必要がある（改行を挟むと構文エラー——Goの`else`配置規則と似た制約だが、AmiFLには自動セミコロン挿入が無いため理由は異なる。単に構文の曖昧性を避けるための判断）。`if`の条件式の中で裸の複合リテラル（`Name{...}`のような、識別子の直後に`{`が続く式）を書くと、if本体の開始`{`と区別が付かなくなるため、条件式中は複合リテラルを直接書けない——丸括弧で包む（`if (Point{x:1,y:2} == other) { ... }`）ことで回避できる。

```amifl
if score >= 90 { "A" } elif score >= 70 { "B" } else { "C" }
```

### 7.2 `switch`（2つの構文形がある）

専用の`match`構文は追加しない（原則3）。`switch`キーワードの直後が`{`かどうかで2つの構文形へ分岐する：

1. **サブジェクト無し・`Bool`専用形**（`switch { case cond1: e1  case cond2: e2  default: e3 }`）：意味的には`if cond1 { e1 } elif cond2 { e2 } else { e3 }`と完全に同一（構文糖衣として`IfExpr`のチェーンへ展開される。専用の型検査コードは存在しない）。`default`は`if`の`else`と同じルールで、式として使う（`Unit`以外の値を返す）場合は省略できない。
2. **サブジェクトあり・`enum`専用形**（`switch subject { case EnumType.Variant: e1  case EnumType.Variant2(field): e2 }`）：`subject`は`enum`型の式でなければならない。case節は`EnumType.Variant`（フィールド無し）または`EnumType.Variant(name1, name2, ...)`（フィールドあり）のみ書ける——**パターン中の識別子は、必ずそのバリアントが宣言しているフィールド名そのものでなければならない**（別名は使えない。別名が欲しければcase本体側で`let`し直す）。全バリアントを1回ずつ網羅していれば`default`は省略可（コンパイル時にバリアント集合が閉じていることを保証できるため）。全バリアント網羅+`default`両方書くことも許される。

**`is Type`パターン、`in [...]`パターンは実装されていない**（旧版は書けるかのように記述していたが誤り）——サポートされるcase節は上記2形式のみ。

```amifl
let label = switch r {
    case Status.Ok: "成功"
    case Status.Retry(delay): "リトライ待ち"   // delayはここでInt値としてcase本体スコープに束縛されている
    case Status.Failed(reason): "失敗: " + reason
}
```

### 7.3 `for`（4つの形がある）

| 形 | 対象型 | 値 | 備考 |
|---|---|---|---|
| `for x in items { ... }` | `List[T]` `Array[T;N]` `Set[T]` `Range` `Stream[T]` | `Unit` | 添字ベースのインクリメント優先ループへ下がる（`List`/`Array`）、または`MPKEYS`経由（`Set`）、または`From`〜`To`を直接数える専用ループ（`Range`——添字を介さず、カウンタ自身が束縛変数の値になる）、または`CHRECV`のterminationチェックループ（`Stream`） |
| `for k, v in m { ... }` | `Map[K,V]`のみ | `Unit` | 2変数形。**`Map`専用**——`List`/`Array`/`Set`/`Range`をこの形で反復するのは型エラー |
| `for x in items yield expr` | `List[T]` `Array[T;N]` `Range` | 反復対象と同じ形の`List[U]`（`items |> map(x => expr)`相当の糖衣。ただし実際の実装は専用コード生成であり`map`関数を経由しない） | **`Set`/`Map`/`Stream`はyield形をサポートしない**（`Stream`は静的な長さを持てないため、`for x in stream yield ...`はコンパイルエラー——`collect(s) |> map(...)`で代替する。`Set`/`Map`は仕様上yield形自体を定義していない）。`Range`は`To - From`が実行時に直接計算できる（`?len`相当の呼び出しすら不要）ため、`Stream`と違いyield形をサポートする——結果`List`のSLMAKE長は0未満にならないよう`max(0, To-From)`へ実行時クランプされる（`From > To`は空`List`になるだけで、パニックにはならない） |
| `for k, v in m yield ...` | — | — | **未実装（構文エラー）**。2変数形とyieldの組み合わせは意図的にサポート外 |

`break`/`continue`は`yield`が無い形でのみ使用可（`yield`式の中では、たとえ構文的に外側の`while`にネストしていても、常に「ループの外」としてコンパイルエラーになる）。`yield`式はUnit型を返せない（`for x in xs yield print(x)`のような呼び出しは拒否される）。

### 7.4 `while`

```amifl
while cond { ... }
```

`while`式自体は常に`Unit`型。`break`/`continue`は最も内側のループのみに作用し、クロージャー境界を越えられない。`break`/`continue`自身はコンパイラ内部の`Never`型に解決される（2.2節、ex11）ため、ループ本体の途中の文としてだけでなく、if/switchの分岐の末尾として他の実型の分岐と混在させることもできる（`if done { break } else { 5 }`）。

---

## 8. 関数とクロージャ

### 8.1 基本構文

```amifl
fn add(a: Int, b: Int) -> Int { a + b }
let square = fn(x: Int) -> Int { x * x }
```

- 本体最後の式が戻り値。早期に脱出したい場合は`return`/`return expr`が使える（5節、ex11）。
- 多引数がそのまま多引数（**カリー化はしない**。パイプ`|>`＋`_`プレースホルダーで部分適用相当ができるため）。
- **可変長引数は無い**。複数値は`List`/`Tuple`にまとめて渡す。

### 8.2 戻り値は常に単一の型

`-> T`のみ。複数値は`TupleN`で包む。「値+エラー」は必ず`Tuple2[T, Error]`という統一形。この制約は`?`演算子とパイプ演算子が「単一の値が流れる」モデルを前提にしているために必要（原則6）。

### 8.3 関数型

`fn(T1, T2, ...) -> R`（2.2節参照）。ex3で型注釈構文を獲得し、`let`・`fn`/クロージャーの引数・戻り値・`struct`のフィールドなど、通常の型注釈が書ける場所であればどこでも書ける——引数無しは`fn() -> R`。戻り値が無い（`Unit`）関数値は`fn(T1, ...) -> Unit`と書く（8.2節の`-> Unit`と同じ規約）。

```amifl
fn apply(f: fn(Int) -> Int, x: Int) -> Int { f(x) }
fn makeAdder(n: Int) -> fn(Int) -> Int {
    let f = fn(x: Int) -> Int { x + n }
    f
}

fn double(n: Int) -> Int { n * 2 }

fn main() -> Int {
    let addThree = makeAdder(3)
    apply(double, 5) + apply(addThree, 5)   // 10 + 8 = 18
}
```

クロージャーリテラル（`fn(x: Int) -> Int { ... }`）自身は**`let`の直接の値**、または**`|>`の右辺**（ex4、9節参照）としてのみ書ける——通常の関数呼び出しの引数へインラインで渡す・別の式へ埋め込む、といった用途は依然として未実装のまま（一度`let`で名前へ束縛してから、その名前を渡す）。`let`の値として書く際は、明示的な`fn(...)->R`型注釈を添えてもよい（クロージャー自身の自己推論した型と一致しなければコンパイルエラー）——省略した場合、これまでどおりクロージャー自身の宣言から完全に推論される。

値としての`Func`はクロージャーに限らない。**トップレベル`fn`をその名前で直接値として参照できる**（`let f = add`、`apply(add, 5)`のように、宣言済みの`fn`名をそのまま渡す）——これが「真の高階関数」（HOF）を可能にする：`apply`のような、Func型のパラメータを受け取り呼び出す関数自体は、渡された値がクロージャーかトップレベル`fn`かを一切区別しない。ただし呼び出しの構文自体（`(fn(x: Int) -> Int { x })(5)`のような、式の結果を直接呼ぶ形）は無い——`f(x)`の`f`は常に裸の名前でなければならない。

関数値どうしの`==`比較は不可。

### 8.4 引数の渡し方

名前付き（ラベル付き）引数、デフォルト値付き引数は提供しない。引数の意味を呼び出し側から分かりやすくしたい場合は、`struct`にまとめて渡す。

```amifl
struct SortOpt { key: String, desc: Bool }
fn sortBy2(xs: List[Int], opt: SortOpt) -> List[Int] { xs }  // 例
```

### 8.5 名前付き`fn`宣言はトップレベルのみ

関数内でローカルに欲しい場合はクロージャー（`let f = fn(...) -> R {...}`）を使う。

```amifl
fn outer() -> Int {
    let inner = fn(x: Int) -> Int { x + 1 }   // OK
    inner(1)
}
```

### 8.6 再帰

トップレベルの名前付き`fn`は自分自身を再帰呼び出しできる（相互再帰も可、`fn`は宣言順に依存せず前方参照できる——シグネチャ収集→本体検査の2パスで解決される）。無名クロージャーは自分の名前を持たないため素朴な自己再帰は不可（トップレベルの名前付き`fn`を経由する）。

### 8.7 関数オーバーロードは無い

同名`fn`の複数宣言は型・数が違っても不可（原則4）。

### 8.8 高階関数

`map`/`filter`/`reduce`/`sortBy`（13.4節）へ渡す関数は、`let`束縛のローカルクロージャーでも、トップレベル`fn`をその名前で直接値として渡す（8.3節、ex3）のでもよい——どちらも同じ`Func`型として扱われる。

```amifl
let isEven = fn(x: Int) -> Bool { x % 2 == 0 }
let evens = filter(xs, isEven)

fn double(x: Int) -> Int { x * 2 }
let doubled = map(xs, double)   // トップレベルfnを直接渡せる
```

---

## 9. パイプ演算子 `|>`

`_`を明示すればその位置へ、省略時は第1引数へ左辺値を注入する。`_`は1回の呼び出しにつき高々1つしか書けない（左辺の式が二重評価されるのを防ぐため——AmiFLには式の途中で値を一度だけ評価してキャッシュする仕組みが無い）。

```amifl
data |> trim |> upper |> print
data |> slice(_, 0, 5)
5 |> addN(_, 3)
5 |> addN(3, _)
```

右辺にインラインクロージャーを直接書く形（`|> fn(x) -> R {...}`、ex4で実装）も使える。左辺値がクロージャーの唯一の引数として渡される——`_`や複数引数の指定はできない（クロージャー自身の引数リストが既に全てを語っているため）：

```amifl
data |> fn(x) -> Int { x * x }
5 |> double |> fn(x) -> Int { x + 1 }
```

`|>`は基本的にパース時の構文糖衣として実装されている——`a |> f`はパーサーが直接`f(a)`という`CallExpr`へ組み立てるだけで、意味検査・コード生成のどちらも`|>`という概念自体を一切知らない。インラインクロージャーの形だけは例外で、`CallExpr.Callee`が常に裸の名前という制約に収まらないため（呼び出し対象が名前ではなくクロージャーリテラルそのもの）、専用フィールド（`CallExpr.InlineClosure`）を持つ——それでも意味検査は`let`が束縛するクロージャーと全く同じ経路（`resolveClosureLit`）で型を確定させ、コード生成も同じ`genClosureLitInto`で一時変数へクロージャーを構築してから呼び出すだけで、新しいAMIVM命令は一切必要としない。クロージャーリテラルを`|>`の右辺以外の位置（呼び出し引数など）にインラインで書くことは依然できない——8.3節の「クロージャーリテラルは`let`の直接の値としてのみ書ける」という制約は、この`|>`右辺という1箇所の例外を除いて変わらない。

### 9.1 パイプラインDX機能

パイプチェーンの途中で型不一致が起きた場合、各ステージのラベル（呼び出しの関数名、または値なら`<value>`）を`|>`で繋いだ再構成済みの1行と、直前ステージの出力型・当該ステージの期待型をエラーメッセージへ含める：

```
error: pipeline type mismatch at stage 3 (data |> parse |> validate |> toUpper)
  stage 2 (validate) output type: Bool
  stage 3 (toUpper) expects: String
```

（**旧版が示していた、ソースコードをcaret付きで逐語的に再現する表示や`hint: did you mean ...`という修正候補提案は実装されていない**——ソースをAST経由で復元する機構がコンパイラに元々存在しないため、実装コストに見合わないと判断し見送られた。）

この番号付き表示が付くのは、期待型があらかじめ固定されている呼び出し（ユーザー定義`fn`/クロージャーの引数、および`trim`/`upper`/`lower`・`split`/`join`/`replace`・`startsWith`/`endsWith`のような固定でString型を期待する少数の組み込み関数）のみ——`len`/`contains`/`map`/`filter`のような「第1引数の型でcapabilityグループを判定する」組み込み関数は対象外（そもそも固定の期待型を持たないため）。`print`はex6で期待型が`String`から`Any`へ一般化されたため、型不一致自体が起こり得ず、この番号付き診断も実質的に発火しない（`typeName`と同じ扱い）。

`tap`/`peek`（13.8節）もパイプラインの開発体験を支える組み込み関数。

### 9.2 組み込み関数の引数順序は「データ第1引数」の慣例で統一する

13節の組み込み関数一覧はほぼ全てデータを第1引数に置く。`_`省略時は必ず左辺値が第1引数へ注入されるため（9節冒頭）、この慣例を組み込みライブラリ全体で一貫させることで、パイプラインの最も一般的な書き方（`data |> f(otherArgs...)`）が`_`を一度も書かずに済む。

---

## 10. `switch` ＋ 組み込み関数によるパターンマッチ（詳細）

7.2節で述べた2形式（サブジェクト無しBool専用形／サブジェクトありenum専用形）の詳細は7.2節を参照。ここではenum形の補足のみ記す。

```amifl
enum Status { Ok  Retry(delay: Int)  Failed(reason: String) }

fn describe(s: Status) -> String {
    switch s {
        case Status.Ok: "成功"
        case Status.Retry(delay): "retry"
        case Status.Failed(reason): "failed"
    }
}
```

`Status.Retry(delay)`と書くとフィールド`delay`がcase本体スコープで束縛される。全バリアントを1回ずつ網羅していれば`default`省略可（コンパイル時にバリアント集合が閉じているため漏れは検出できる）。

---

## 11. 並列処理

専用構文を増やさず、`Chan[T]`／`Stream[T]`という「型」＋既存の`for`＋パイプの組み合わせだけで表現する。

- `chan[T](buffer: Int) -> Chan[T]`：バッファ付きチャネルを作る（組み込み関数、キーワードではない）。
- `send(ch, v) -> Unit` / `recv(ch) -> Tuple2[T, Bool]`：チャネル送受信（`<-`のような専用記号は導入しない、原則3）。
- `spawn(f: fn() -> Unit) -> Unit`：goroutine起動。
- `parallel(s: Stream[T], workers: Int) -> Stream[T]`：N個のgoroutineによる**中継のみ**を行う（変換関数を取らないため、`s`から受信した値をそのまま`out`へ転送するだけ）。全ワーカーの完了後にチャネルを閉じる。値の変換をしたい場合は`parallel`の前後で`map`等と組み合わせる。
- `take(s, n) -> Stream[T]` / `skip(s, n) -> Stream[T]` / `collect(s) -> List[T]`：Streamの部分取得・全消費（13.8節）。
- **チャネル（`chan[T]`で作った生のChan）は一度開いたら明示的に閉じる組み込み関数が存在しない**——`recv`の`ok`フラグによる終了検出が成立するのは、`lines`/`take`/`skip`/`parallel`のようにコンパイラ自身が内部的にチャネルを生成し、自身の判断で閉じる`Stream[T]`系の関数群だけである。

---

## 12. モジュールと複数ファイルの統合

### 12.1 パッケージ＝ディレクトリ

同じディレクトリに置かれた`.aml`ファイル群は、1つの「パッケージ」として1つのプログラムへ統合される。パッケージ**内**のファイル間では`import`は不要——全てのファイルが1つの共有スコープにあるかのようにコンパイルされる（トップレベルの`fn`／`struct`／`enum`／`const`宣言を無条件に共有する）。

```
myproject/
    main.aml
    reading.aml
```

コンパイル対象として**単一ファイル**を直接指定した場合は、そのファイル1つだけの独立したパッケージとして扱う（同じディレクトリに他の`.aml`ファイルがあっても無視する）。`.amlz`アーカイブ（16.2節）はディレクトリの代役として全く同じ扱いを受ける。

### 12.2 パッケージをまたぐ参照

別のディレクトリにあるパッケージを参照したい場合は`import <エイリアス> "<相対パス>"`を使う（パスは文字列リテラルのみ、コンパイル時解決）。パスは参照元ファイル自身のディレクトリからの相対パス。`import`文はトップレベルであればファイル中どこに書いてもよい。

```amifl
import mathutil "./mathutil"

fn main() -> Int {
    mathutil.Clamp(15, 0, 10)
}
```

**他パッケージから参照できるのは、名前が大文字で始まるトップレベル宣言だけ**（Goの可視性規則を採用）。**値の種類（`fn`／`struct`／`enum`／`const`）は問わない**——`struct`／`enum`のクロスパッケージ参照（`alias.Point{...}`のような構築、`alias.Status.Variant(...)`のような値生成、`alias.Name`という型注釈）はex5で実装された（Step 14の時点では`fn`/`const`のみに意図的に絞られていたが、構文的なコスト——`struct`のクロスパッケージ構築には複合リテラルの曖昧性回避、`enum`には2段ドットのチェーンが要る——は、パーサーの既存の後置`.`連鎖ループがどちらも無変更で生成できることが分かり、想定より小さく済んだ）。

```amifl
// mathutil/mathutil.aml
fn Clamp(v: Int, lo: Int, hi: Int) -> Int {
    if v < lo { lo } elif v > hi { hi } else { v }
}
const MaxClamp: Int = 100

struct Interval {
    lo: Int,
    hi: Int
}

enum ClampOutcome {
    Unchanged
    Adjusted(delta: Int)
}

fn double(x: Int) -> Int { x * 2 }   // 小文字始まりなので他パッケージから見えない
```

```amifl
import mathutil "./mathutil"

fn main() -> Int {
    let iv: mathutil.Interval = mathutil.Interval{lo: 0, hi: 10}
    mathutil.Clamp(15, iv.lo, iv.hi)
}
```

`エイリアス.Name`という参照（関数呼び出し・`const`参照・`struct`構築・型注釈）は、3.2節のタプル／構造体フィールドアクセスの`.`と同じ`FieldExpr`ノードを再利用しつつ、コンパイル時に対象パッケージの宣言へ直接解決される。`mathutil.Clamp(15, 0, 10)`は`Clamp(15, 0, 10)`という素直な呼び出しへコンパイル時に変換される（実行時の名前検索は一切経由しない）。名前付き引数を渡すような呼び出し形はここでは使えない（8.4節どおり存在しないため）。`alias.EnumType.Variant(...)`（enum構築）は同じ`.`連鎖がもう1段重なっただけの形——専用の構文は無い。

**クロスパッケージのenum値を`switch`で直接パターンマッチすることはできない**（意図的なスコープ限定）——ケースパターン（10節）は同じパッケージ内のenum名しか書けない構文のまま据え置かれている。あるenumの値を外部から検査したい場合は、そのenum自身の宣言パッケージ側に検査用の関数（内部で`switch`する）を用意し、それを呼び出す形にする。

### 12.3 `main`はルートパッケージでのみエントリーポイントになる

`main`（14節）は、コンパイル時にルートとして指定したパッケージ（通常はプロジェクト最上位のディレクトリ、または直接指定した単一ファイル）に書いたものだけが実際のエントリーポイントとして扱われる。`import`で取り込まれている側（非ルートパッケージ）が偶然`main`という名前をトップレベルで使っていても、コンパイルエラーにはしない——他の名前と同じように内部的な私的束縛になるだけで、エントリーポイントとしては一切扱われない。

### 12.4 コンパイル時の名前解決

複数ファイル・複数パッケージは、実行が始まる前に**1つのプログラムへ静的に統合**される。非ルートパッケージのトップレベル宣言（公開・非公開を問わない）は全て、一意な名前へ機械的に置き換えられる。**この置き換えは`import`文で与えたエイリアスそのものを使う**（ディレクトリ名ではない）——別々の場所にある2つのパッケージが偶然同じディレクトリ名を持っていても、別のエイリアスで取り込む限り衝突しない。

**同じエイリアスを2つの異なるパッケージへ割り当てようとすると、コンパイルエラーになる。** これはプログラム全体で厳格に適用される——1つのファイル内での再割り当てだけでなく、別々のパッケージがそれぞれ独立に同じエイリアス名を選んでしまった場合も拒否される（プログラム全体で1つのグローバルなエイリアス→パッケージ表を持つ）。

逆に、同じパッケージが複数の非ルートパッケージからそれぞれ違うエイリアスで`import`された場合、そのパッケージ自身の宣言をリネームする際に使う正規プレフィックスは「最初にそのパッケージへ到達した際のエイリアス」に決定的に固定される（ファイル探索順序・import宣言順序は決定的なので再現可能）。

### 12.5 パッケージ間の依存

パッケージ間の依存は有向非巡回グラフでなければならない（循環禁止、自己循環も含む）。

---

## 13. 組み込み関数 完全一覧

可変長引数は一切持たない。組み込み関数はいずれも**インポート不要でグローバルスコープに存在**する。呼び出し箇所での優先順位は「組み込み関数 > 同名のユーザー宣言」——同名の`fn`/`let`を宣言すること自体はエラーにならないが、呼び出し時は常に組み込みが優先される。

| 領域 | 該当区分 |
| :--- | :--- |
| I/O・ストリーム領域 | 13.1（出力・終了）、13.8（チャネル・ストリーム・並列）、13.10（ファイルI/O） |
| データ操作領域 | 13.4（データ操作）、13.5（集合）、13.6（Map） |
| 型・基礎操作領域 | 13.2（型・値判定）、13.3（変換）、13.7（数値） |

### 13.1 出力・終了

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `print` | `(v: Any) -> Unit` | 標準出力へ`v`の`%v`書式（Goの汎用フォーマッタ、`typeName`・`tap`/`peek`と同じ）で書き、末尾に改行。`v`は`Unit`型不可（ex6で`String`専用から`Any`へ一般化——2.2/13.2節の「無制約な多相引数、モノモーフィックに解決される」`Any`であり、`extern`境界の動的`Any`ではない点は`typeName`/`isError`と同じ） |
| `eprint` | `(v: Any) -> Unit` | `print`の標準エラー出力版。書式・`v`の制約は`print`と同一 |
| `format` | `(v: Any) -> String` | `print`が書き出すのと同じ`%v`書式の文字列を、書き出す代わりに値として返す |
| `formatWith` | `(template: String, v: Any) -> String` | `template`中の最初の`{}`を`v`の`%v`書式文字列で置き換えて返す（`{}`が無ければ`template`をそのまま返す）。AmiFLは可変長引数を持たない（原則7）ため1回の呼び出しで埋められるのは1箇所のみ——2箇所以上埋めるには`|>`で連鎖させる（各呼び出しが最も左の残った`{}`を1つ消費する）：`"{} and {}" |> formatWith(_, a) |> formatWith(_, b)` |
| `exit` | `(code: Int) -> Unit` | プロセスを即座に終了コード`code`で終了する（Goの`os.Exit`、`defer`は実行されない）。`Unit`型固定——Goの`panic`のように「あらゆる期待型に構造的に適合する」設計にはしていない（17.2節参照）。`print`/`eprint`と同じく`Unit`が許される場所（文位置、または全分岐が`Unit`型の`if`の1分岐）でのみ使える |

### 13.2 型・値判定

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `typeName` | `(v: Any) -> String` | `extern`由来の`Any`値を含む、あらゆる値の動的型名（Goの`%T`書式）を返す |
| `isError` | `(v: Error) -> Bool` | 値が非`nil`の`Error`かどうか |

### 13.3 変換

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `cast[T]` | `(v: Numeric) -> T` | 数値型どうしの明示変換 |
| `parse[T]` | `(s: String) -> Tuple2[T, Error]` | 文字列→数値/真偽値へのパース（`T`は符号付き/符号無し整数・浮動小数点・`Bool`） |

### 13.4 データ操作（多相組み込み関数、2.4節参照）

| 関数 | シグネチャ | 対象型 |
|---|---|---|
| `len(x) -> Int` | | `String`, `List`, `Array`, `Map`, `Set`, `Chan`（`Bytes`は`List`経由） |
| `slice(x, from, to) -> String\|List[T]\|Stream[T]` | 3引数常に必須（`_`省略形はこの関数呼び出し形では使えない） | `String`, `List`, `Array`, `Stream` |
| `at(x, i) -> T` | | `List`, `Array` |
| `setAt(x, i, v) -> Unit` | | `List`のみ（固定長`Array`は`x[i]=v`のみ） |
| `contains(x, target) -> Bool` | | `String`（部分文字列）, `List`, `Array`, `Map`（キー）, `Set` |
| `index(x, target) -> Tuple2[Int, Bool]` | | `String`, `List`, `Array`（`Map`/`Set`は順序不定のため対象外） |
| `split(s, sep) -> List[String]` / `join(xs, sep) -> String` | | `String` |
| `replace(s, old, new) -> String` / `trim(s) -> String` / `upper(s) -> String` / `lower(s) -> String` | | `String` |
| `startsWith(s, prefix) -> Bool` / `endsWith(s, suffix) -> Bool` | | `String` |
| `map(xs, f: fn(T)->U) -> List[U]` | | `List`, `Array`（**`Stream`/`Chan`は未サポート**——旧版の記述は誤り） |
| `filter(xs, f: fn(T)->Bool) -> List[T]` | | `List`, `Array`（同上、`Stream`/`Chan`未サポート） |
| `reduce(xs, init: U, f: fn(U,T)->U) -> U` | 累積値が第1引数（fold-left） | `List`, `Array`（同上、`Stream`未サポート） |
| `sort(xs) -> List[T]` | `T`は`Ordered` | `List`のみ |
| `sortBy(xs, opt: fn(T)->K) -> List[T]` | `K`は`Ordered`（キー抽出クロージャー） | `List`のみ |
| `reverse(xs) -> 同じ形` | 長さを変えない | `List`, `Array`, `String`（ルーン単位、UTF-8を破壊しない） |
| `unique(xs) -> List[T]` | `T`はEqualityComparable | `List`のみ |
| `flatten(xs: List[List[T]]) -> List[T]` | | `List`のみ |
| `zip(xs, ys) -> List[Tuple2[A,B]]` | | `List`のみ |
| `push(xs, v) -> List[T]` | **非破壊**（新しいListを返す） | `List`のみ |
| `pop(xs) -> Tuple2[List[T], T]` | `.0`=残りのList, `.1`=取り出した最後の要素。非破壊 | `List`のみ |
| `insert(xs, i, v) -> List[T]` | 非破壊 | `List`のみ |
| `removeAt(xs, i) -> List[T]` | 非破壊 | `List`のみ |
| `concat(a, b) -> 同じ型` | | `String`, `List`（`Bytes`は`List`経由で対象） |

### 13.5 集合(`Set[T]`)専用

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `add(s, v) -> Unit` | **破壊的**（対象を直接書き換える） | 要素追加 |
| `discard(s, v) -> Unit` | 破壊的 | 要素削除（無ければ何もしない） |
| `union(a, b)` / `intersect(a, b)` / `difference(a, b) -> Set[T]` | 非破壊、新しいSetを返す | 集合演算 |
| `toList(s) -> List[T]` | | 順序不定（`MPKEYS`をそのまま使う） |

### 13.6 `Map[K,V]`専用

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `keys(m) -> List[K]` | | `Set.toList`と同一実装（`MPKEYS`） |
| `values(m) -> List[V]` | | |
| `entries(m) -> List[Tuple2[K,V]]` | | |
| `get(m, k, default: V) -> V` | | 無ければ`default` |
| `set(m, k, v) -> Unit` | **破壊的** | |
| `delete(m, k) -> Unit` | 破壊的 | 無ければ何もしない |

**`Set`/`Map`のミューテーターは破壊的**（`add`/`discard`/`set`/`delete`）、**`List`のミューテーター相当は非破壊**（`push`/`pop`/`insert`/`removeAt`）——両者は対称ではない。実行時表現がどちらも既にmapという参照型であることに加え、`List`の`append`はバッキング配列を再利用しうる（別のAmiFL変数が同じ配列を静かに共有してしまう危険）ため、`List`側だけ常にコピーしてから変更する設計にした。

### 13.7 数値

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `min(a, b)` / `max(a, b)` / `abs(v)` / `clamp(v, lo, hi)` | いずれも`Numeric` | 型非依存の`IF`/`ELSE`直接生成、amiflrt不要 |
| `round(v)` / `floor(v)` / `ceil(v)` | `Float`のみ | `Float32`は`float64`経由で往復キャストされる |
| `pow(base, exp)` / `sqrt(v)` | `Float`のみ | 同上 |

### 13.8 チャネル・ストリーム・並列

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `chan[T]` | `(buffer: Int) -> Chan[T]` | |
| `send` / `recv` | `(ch, v) -> Unit` / `(ch) -> Tuple2[T, Bool]` | `Chan[T]`専用 |
| `spawn` | `(f: fn() -> Unit) -> Unit` | goroutine起動 |
| `parallel` | `(s: Stream[T], workers: Int) -> Stream[T]` | Nワーカーによる中継のみ（11節参照） |
| `collect` | `(s: Stream[T]) -> List[T]` | |
| `take` / `skip` | `(s: Stream[T], n: Int) -> Stream[T]` | |
| `tap` | `(v: T, label: String) -> T` | 恒等関数。**常に**stderrへ`[label] value`形式で1行出力（本番実行でも動く）。`T`に制約は無い（無制約多相、13.9節の`unwrap`/`okOr`を除けば唯一の完全無制約T） |
| `peek` | `(v: T) -> T` | 恒等関数。環境変数`AMIFL_DEV`が空なら完全にI/Oを行わないパススルー。非空なら動的型名＋値をstderrへ表示し、標準入力の1行（Enterキー）を読むまでブロックする対話的インスペクタ |

`v`がUnit型の場合、`tap`/`peek`いずれも明示的にコンパイルエラーになる。

### 13.9 エラー処理

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `unwrap[T]` | `(t: Tuple2[T, Error]) -> T` | エラーが非nilならGoの`panic`を呼ぶ（**プロセスが実際にクラッシュする**——stderrへトレース、終了コード2。プロトタイピング用途、本番コードでの多用は推奨されない） |
| `okOr[T]` | `(t: Tuple2[T, Error], default: T) -> T` | エラーなら`default`を返す |

いずれも`[T]`ブラケット型引数は省略可能（`t`自身の型が`T`を既に決定しているため）。

### 13.10 ファイルI/O

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `open` | `(path: String, mode: String) -> Tuple2[File, Error]` | `mode`は`"r"` `"w"` `"a"` |
| `close` | `(f: File) -> Error` | |
| `read` | `(f: File, n: Int) -> Tuple2[Bytes, Error]` | |
| `readAll` | `(f: File) -> Tuple2[Bytes, Error]` | |
| `readLine` | `(f: File) -> Tuple2[String, Error]` | 行末の`\n`（および`\r`）は含まれない |
| `lines` | `(f: File) -> Stream[String]` | |
| `write` | `(f: File, data: String\|Bytes) -> Tuple2[Int, Error]` | `data`は`String`・`Bytes`のどちらでも渡せる（ex12。以前は`Bytes`専用で、文字列を書くには数値のバイト列を手で綴る必要があった） |
| `stdin` / `stdout` / `stderr` | `() -> File` | |

---

## 14. エントリーポイント

```amifl
fn main() -> Int {
    print("Hello, AmiFL!")
    0
}
```

```amifl
fn main(args: List[String]) -> Int {
    len(args)
}
```

`main`はルートパッケージにのみ書ける予約名で、**2つのシグネチャのどちらか一方のみ**が有効：引数無し（`fn main() -> Int`）、または`args`という単一の`List[String]`引数を取る形（`fn main(args: List[String]) -> Int`）。後者の`args`は、実行ファイル自身の名前（Goの`os.Args[0]`相当）を含まない、プログラムへ渡されたコマンドライン引数そのもの（`os.Args[1:]`）。2個以上の引数、あるいは`List[String]`以外の型の単一引数はコンパイルエラーになる。戻り値の`Int`（`Int64`）はプロセスの終了コードとしてOSへ渡される（Goの`os.Exit`が要求する固定幅ではない`int`へ、内部的に1回明示キャストしてから渡す）。

---

## 15. `extern`機構（Go資産の利用）

### 15.1 関数のバインド

```amifl
extern "encoding/json" as json2 {
    bind Marshal2(v: Any) -> Tuple2[Bytes, Error] as Marshal
}
```

`extern "パス" as 名前空間 { type Name  bind ... }`はGoパッケージをホワイトリスト方式で取り込む。`bind`されたGo関数は、`名前空間.関数名`のような修飾なしに、AmiFLのグローバルスコープへ直接束縛される裸の名前として呼び出す（`.`を使った呼び出し構文は存在しない）。`名前空間`（`as`の右辺）はAmiFLソース内では一切参照されず、コンパイラが生成コードに挿入するGo importのエイリアスとしてのみ使われる。

`bind`のGo側の実際の呼び出し対象は、既定では`Name`（bind名）と完全に同じ名前のパッケージレベル関数だが、`as GoTarget`で明示的に指定できる。`GoTarget`は次の2形式のいずれか：

- 裸の識別子（例: `as Marshal`）：`Name`とは異なる名前のパッケージレベル関数を指す
- `Type.Method`（例: `as Time.Unix`）：15.2節のメソッドバインド専用

### 15.2 Goのメソッドの扱い

`method`宣言のようなGoのレシーバ構文を持つ専用構文は設けない。Goのレシーバ付きメソッドは、レシーバを普通の第1引数として受け取る関数として`bind`する。

```amifl
extern "time" as time {
    type Time
    bind Now() -> Time
    bind TimeUnix(t: Time) -> Int as Time.Unix
    bind TimeFormat(t: Time, layout: String) -> String as Time.Format
}

fn todayString() -> String {
    Now() |> TimeFormat(_, "2006-01-02")
}
```

`Type.Method`の`Type`は`bind`の**第1引数の宣言型と完全一致**必須（一致しなければコンパイルエラー）。内部的にはAMIVMの`METHVAL`命令（`local := receiver.method`）でメソッド値を取得してから残りの引数だけを渡す、という2命令構成でコンパイルされる（AMIVMの`callname`オペランドがドット1個までしか許さない制約——`?pkg.Type.Method`という3要素のドット区切りを直接は書けない——を、この2命令構成で回避している）。

`extern "path" as alias { type Name }`は、AmiFL側から`Name`という裸の型名で参照できる不透明な外部型を1つ登録する（Go側の型名と常に同一、リネームは無い）。フィールド・メソッド呼び出し構文は無い。比較可能性はGo側の実際の型に依存し、AmiFL側semaは静的に検証できない（既知の限界、2.3節参照）。

### 15.3 複数文にまたがるGoロジックが必要な場合

単一の関数・メソッド呼び出しに収まらない複雑なGoロジックが必要な場合も、専用構文は用意しない。同じディレクトリに手書きの`.go`ファイルを置き、そこで定義した関数を`extern`で束ねる、という15.1節と同じ経路に統一する。

### 15.4 `Any`型境界

`bind`宣言の引数・戻り値に`Any`が現れる場合、実行時表現はGoの`any`そのもの——Goの暗黙のインターフェース変換にそのまま乗るだけで、`reflect`や専用の型アサーション命令は一切不要だった（`Any`値を絞り込む手段がAmiFLに無いため）。整数リテラルを直接`Any`パラメータへ渡す場合（`someExternFn(5)`）は、Goの無型定数がボックス化時に採用するデフォルト型（`int`）とAmiFLのデフォルト型（`Int64`）の食い違いを避けるため、コンパイラが自動的に`?int64`への明示キャストを1回挟む。

### 15.5 コンパイラ組み込み型の扱い

`File`・`Chan[T]`のようなコンパイラ組み込み型はユーザーが`extern`で独自に上書きすることはできない（`bind`できるのは名前空間内のGo関数のみ）。`extern`の`as`エイリアスは、コンパイラ自身が生成コードで使っている名前（`os`/`fmt`/`strconv`/`strings`/`math`/`amiflrt`）およびGoの予約語・組み込み識別子全体と衝突することを許さない（予約リストによる防御）。

---

## 16. プロジェクト構成とツールチェイン

### 16.1 ディレクトリ構成

```
amifl/                          AmiFLのホーム（このリポジトリのルート）
  amifl-spec.md                 AmiFL言語仕様（唯一の正）
  amifl_implementation_notes.md AMIVM-IR生成に関する実装知見（次にAMIVM上で言語を実装する人向け）
  CLAUDE.md                     プロジェクト規約・全ステップの確定した設計判断の記録
  LICENSE                       MIT
  go.mod                        module github.com/amisonnet8/amifl
  Makefile                      build/install/test/test-examples/fmt/vet/tidy/clean タスク
  .github/workflows/test.yml    CI: push/PR時にgofmt/go vet/go test/make test-examplesを実行
  cmd/amifl/
    main.go                     CLIエントリポイント（build/run/emit-ir/emit-go/archive/help のディスパッチ）
    build.go                    modloader.Load → sema.Check → codegen.Generate → amivm(-IR生成) →
                                 go build のパイプライン
    archive.go                  `amifl archive` サブコマンド
  internal/lexer/                字句解析
  internal/parser/               構文解析 → AST
  internal/ast/                  AST定義（唯一の共有語彙。semaとcodegenはこれにのみ依存し互いには依存しない）
  internal/modloader/            パッケージ取り込みの解決（12節）
  internal/sema/                 意味検査（型チェック・スコープ解決・capability解決・パイプライン型接続検査等）
  internal/codegen/              AST → AMIVM-IR生成
  amiflrt/                       AmiFL独自ランタイム（go:embedで配布。Stream[T]/Chan[T]/File等の
                                  組み込み型・13節の組み込み関数のうちAMIVM命令に対応しないものの実装）
  examples/                      サンプルAmiFLプログラム（`.aml`）
  tour/                          入門ガイド（11章構成のMarkdown。`README.md`が目次）
```

### 16.2 コマンドライン・インターフェース

```
amifl <command> [flags] <file.aml | package-dir | package.amlz>
```

| コマンド | 説明 |
|---|---|
| `build` | ネイティブ実行ファイルへコンパイルする |
| `run` | コンパイルして即座に実行する（stdin/stdout/stderrをストリームする） |
| `emit-ir` | AMIVM-IRへコンパイルする |
| `emit-go` | Goソースへコンパイルする（amivm経由） |
| `archive` | ディレクトリ直下の`.aml`ファイル群を`.amlz`アーカイブへまとめる |
| `help` | ヘルプメッセージを表示する |

`build`／`emit-ir`／`emit-go`共通フラグ：

| フラグ | 説明 |
|---|---|
| `-o <file>` | 出力ファイルパス（省略時は入力パスから導出） |
| `-v` | 各パイプライン段階（AMIVM-IR・型チェック過程・最終生成コード）の出力を表示する |

`archive`のフラグ：`-o <file>`（省略時は`<ディレクトリ名>.amlz`）。

`.amlz`はプレーンなzipファイル（ディレクトリ直下の`.aml`ファイル群をフラットに格納するだけ）——`import`/コンパイルのトップレベル引数どちらでも、元のディレクトリの完全な代役として扱われる（そのアーカイブ自身が置かれている場所を基準に相対`import`を解決する）。

`package-dir`（またはその`.amlz`アーカイブ）を指定した場合、そのパッケージ配下の全`.aml`ファイルを1つのプログラムとしてコンパイルする（12.1節）。単一ファイルを指定した場合は、そのファイル1つだけをコンパイルする（同じディレクトリの他ファイルは無視する）。

---

## 17. 実装されていない機能・既知の限界・未決事項

実装完了後の全面書き直しにあたり、旧版が「書けるように見える」記述をしていたが実際には実装されなかった機能、および設計上の既知の限界を一覧化する。将来これらへ着手する場合は、まず本節を参照し、必要ならCLAUDE.mdの開発方針（8番：大きな変更は着手前にユーザーの明示的な合意を得る）に従うこと。

### 17.1 旧版の記述と実装が食い違っていた項目（本書で訂正済み）

| 項目 | 旧版の記述 | 実際の実装 |
|---|---|---|
| `+`演算子のConcatenable | `String`/`Bytes`/`List`/`Array`に効く | **`String`専用**。`List`/`Bytes`の連結は`concat()`を使う |
| `switch`のパターン | `is Type` / `in [...]` パターンをサポート | **未実装**。Bool専用形（サブジェクト無し）とenum専用形（`Type.Variant(...)`）の2形のみ |
| `map`/`filter`/`reduce` | `Stream`/`Chan`にも適用可 | **`List`/`Array`のみ** |
| パイプラインDX型エラー表示 | ソースのcaret付き再現＋`did you mean`提案 | **簡略版**：ラベル列＋直前/期待型のみ（ソース逆変換機構が無いため） |
| `capability Lenable {...}`構文 | AmiFL構文として掲載 | **AmiFL構文として存在しない**。コンパイラ内部限定の分類（`capability`はキーワードですらない） |

### 17.2 意図的な既知の限界（設計判断として確定済み）

1. `Set[T]`の要素順序は不定（`toList(_) |> sort`で決定的にする）。`OrderedSet`は追加しない方針。
2. `Stream[T]`と`Chan[T]`の間の暗黙変換は無い。明示変換関数（`toStream(ch)`等）も未実装——具体的な必要が出た時点で追加を検討する。
3. `?`演算子は`Tuple2[T,Error]`（またはError単体）専用で、3値以上への一般化は行わない方針。
4. `extern`のバインドがGo側APIの変更と食い違った場合の検知の仕組みは無い（バージョニング機構は無し）。
5. `extern`型（15節）の`==`比較可能性はAmiFL側semaでは静的検証できない（Go側の実際の型に依存、既知の限界として許容）。
6. タプルのフィールド（`.0`等）は代入できない（ex10）——タプルは不変値として扱う方針（`struct`のフィールド代入とは対照的、2.2/3.2節参照）。
7. ビルドキャッシュ・増分コンパイルの仕組みは無い（本書の対象外）。
8. `unwrap[T]`はエラー時に実際にプロセスをクラッシュさせる（Goの`panic`）。本番コードでの多用は推奨しない（`okOr`か`?`+呼び出し元での処理を推奨）。
9. クロスパッケージのenum値を`switch`で直接パターンマッチすることはできない（ex5）——ケースパターンの構文が同じパッケージ内のenum名しか書けないまま据え置かれている。宣言パッケージ側に検査用の関数を用意して呼び出すこと。
10. `exit`（13.1節）は`Unit`型固定で、Goの`panic`のように「あらゆる期待型に構造的に適合する」Never的な型付けはしていない（ex6）——`if ok { 5 } else { exit(1) }`のような、非`Unit`分岐の代わりに使うことはできない（両分岐が`Unit`の`if`でなら使える）。resolveTypeが`expected`をCallExprの型解決へ渡さない現行の呼び出し解決パイプライン全体を変える必要があり、単一の組み込み関数のためだけに行うには不釣り合いなため、意図的に見送った。
11. `formatWith`は1回の呼び出しで`{}`プレースホルダーを1箇所しか埋められない（可変長引数が無いため、13.1節参照）。複数箇所埋めるには`|>`で連鎖させること。
12. 浮動小数点リテラルの桁区切り`_`（3.1節、ex7）はコンパイラ内部で常に10進の数値へ再構成されて`amivm`へ渡る——`amivm`自身の桁区切り`_`対応が整数リテラルに限られている（ignored/amivm/amivm_spec.md 6節）ため。挙動に違いは無い（生成される値は同じ）が、`emit-ir`で覗いた生成IRの浮動小数点リテラルには元のソースにあった`_`は残らない。

### 17.3 将来的な拡張の余地（着手する場合はCLAUDE.mdの開発方針8番に従うこと）

- `Range`の一般化：表面型注釈構文（`Range`を`let`/引数/フィールドの型として書けるようにする）、`Int64`以外の整数幅・降順・ステップ指定、`len`/`contains`等13.4節の組み込み関数対応。いずれも現状は意図的なスコープ限定（3.1節参照）。
- クロージャーリテラル自体を、`let`の直接値・`|>`の右辺（ex4で実装済み）以外の位置（通常の呼び出し引数など）へもインラインで書けるようにすること——現状は依然として`let`で一度名前へ束縛してからでないと渡せない（8.3節・9節参照）。
- クロスパッケージのenum値の`switch`パターンマッチ（ex5で構築・型注釈は実装済み。17.2節#9参照）。
- `exit`のNever的な型付け（17.2節#10参照）、`formatWith`の複数プレースホルダー一括指定（17.2節#11参照）——いずれも具体的な必要が出た時点で再検討する。
