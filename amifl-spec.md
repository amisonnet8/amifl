# AmiFL言語 詳細仕様書

> 汎用データ処理特化言語AmiFLの詳細仕様書。`concept.md`の設計方針に基づく。

**ライセンス**: MIT

---

## 0. 前提

- **静的型付け**。値の種類は常にコンパイル時に確定し、実行時の動的ディスパッチは無い。
- 位置づけ：「AWK/Perlの系譜だが、Perl的な乱雑さを避けた汎用データ処理特化言語」（`concept.md` 1節）。
- ファイル拡張子は`.aml`。
- 実行基盤としてGo言語製の仮想マシン（AMIVM）を採用し、最終的にGoコードへ変換して`go build`する（`concept.md`概要）。この変換の内部命令対応は本書の対象外。

---

## 1. 設計思想（一貫して現れるモチーフ）

`concept.md`の「Perl的な乱雑化を避ける」という一番の動機を、以下7つの原則として具体化する。

1. **式指向・文の排除**：プログラムは式の並びのみ。`if`/`switch`/`for(yield)`/`fn`/`let`/`const`/`return`は全て「値を持つ式」。ブロックの値は最後の式。最後以外の式は`Unit`型でなければならない（戻り値の握りつぶし忘れ防止。捨てたいときは明示的に`_ = 式`）。詳細は5節。
2. **暗黙変換の排除**：サイズ・符号違いの数値型は相互に暗黙変換しない。異なる型どうしの`==`もコンパイルエラー。「型が違っても動いてしまう」曖昧さ（Perl的な緩さ）を意図的に排除する。
3. **1つの仕組みで足りるものを2つ用意しない**：多次元配列に専用構文を作らず`Array[T;N]`のネストの糖衣にする、添字も`x[i,j]`のような別構文を導入せず`at`の連鎖にする、チャネル送受信も専用記号（`<-`）を導入せず組み込み関数`send`/`recv`にする、パターンマッチも専用`match`構文を作らず`switch`の拡張にする。これは`concept.md`「専用の`match`構文などを新設して構文を無闇に増やさない」の一般化である。
4. **ユーザー拡張ポイントを絞る**：関数のオーバーロード無し、ユーザー定義capability（多相の型クラス的機構）拡張無し、可変長引数無し、カリー化無し、`struct`/`enum`にメソッドを持たせない。多相が必要な場面はコンパイラ組み込みの多相関数（`len`/`slice`等）として個別対応し、一般的な拡張機構は提供しない。`concept.md`が繰り返し明言する「Perl化を防ぐ」の直接的な具体化。
5. **グローバル可変状態の排除**：トップレベルの`let`（グローバル変数）を禁止。状態共有が必要なら`Chan[T]`を介した明示的な受け渡しを使う。
6. **戻り値は常に単数、複数値はタプルで包む**：「値+エラー」パターンは常に`Tuple2[T, Error]`という1つの形に統一する。これにより`?`演算子やパイプ演算子が前提とする「1つの値が流れる」モデルを保てる。
7. **明示性 > 簡潔さ**：可変長引数を許さず、複数値は`List`/`Tuple`にまとめて渡す。名前付き引数は提供しない（8.4節）。

---

## 2. 型システム

### 2.1 スカラー型

- 符号付き整数：`Int8` `Int16` `Int32` `Int64`（`Int`は`Int64`のエイリアス、既定の整数型）
- 符号なし整数：`UInt8` `UInt16` `UInt32` `UInt64`（`UInt`は`UInt64`のエイリアス）
- 慣用名エイリアス：`Byte`(=`UInt8`)、`Rune`(=`Int32`、Unicodeコードポイント用)
- 浮動小数点：`Float32` `Float64`（`Float`は`Float64`のエイリアス、既定）
- `Bool`、`String`（UTF-8、イミュータブル）、`Bytes`（可変長バイト列、`List[Byte]`と同じ内部表現）
- サイズ・符号の異なる数値型どうしは暗黙変換しない。明示変換は`cast[T](v)`。

### 2.2 コレクション・複合型

- **`Tuple2[A,B]`〜`Tuple8[...]`**：固定長・異種混合。要素数2〜8。リテラルは`(v1, v2, ...)`。1要素だけの場合`(x)`は単なる括弧付き式（グループ化）になってしまうため、1要素タプルは`(x,)`と末尾カンマで書く。フィールドアクセスは`.0` `.1` ...の糖衣。メソッドは無い。
- **`Set[T]`**：重複無し。`T`は比較可能な型（数値・文字列・真偽値・タプル）のみ。リテラル`{1, 2, 3}`。要素の順序は不定。
- **`Array[T; N]`**（固定長・コンパイル時サイズ）と**`List[T]`**（可変長）：明確に2つの型として分離する。理由：`Array`/`List`はメモリ上の実体を持ちランダムアクセス・添字書き込みが要る用途、`Stream[T]`は遅延・前方一方向・ランダムアクセス不可という別の性質を持つため、`Stream`へ統合しない。既定のリテラル`[1,2,3]`は`List[T]`。型注釈で明示したときのみ`Array[T;N]`になる。
  - **多次元配列**`Array[T; N1, N2, ...]`は`Array[Array[...Array[T;Nk]...]; N2]; N1]`のネストの糖衣構文にすぎない（新しい型・新しい構文は増やさない、原則3）。添字も`x[i][j]`の連鎖で足り、`x[i,j]`という別構文は導入しない。
  - コレクションリテラルの型注釈は、多次元配列などネストの各階層へ再帰的に伝播する（`Array[Int;2,2]`への代入で`[[1,2],[3,4]]`と書くと内側も自動的に`Array[Int;2]`と解釈される）。無注釈時は各階層独立に既定（`List`）が適用される。
- **`Map[K,V]`**：連想配列。
- **`struct`**：ユーザー定義。フィールドのみでメソッドは持たない。
  ```amifl
  struct Point { x: Float, y: Float }
  ```
- **`enum`**（タグ付きバリアント）：複数の「形の異なる場合分け」を1つの型として表現する。各バリアントは0個以上の名前付きフィールドを持てる。
  ```amifl
  enum Status {
      Ok
      Retry(delay: Int)
      Failed(reason: String)
  }
  let s2: Status = Status.Retry(delay: 5)
  ```
  値生成は`型名.バリアント名(...)`というリテラルではない通常の式。バリアント判定・フィールド取り出しは`switch`のパターンマッチでのみ行う（10節）。バリアント集合はコンパイル時に閉じている（実行時追加は不可）。`struct`との違いは「バリアントごとに異なる形のフィールド集合を持てる」点のみ。
- **`Stream[T]`**（遅延・ファイル/標準入出力等向け）／**`Chan[T]`**（並列処理用キュー）：`Stream[T]`は内部的に`Chan[T]`＋goroutineの糖衣という位置づけ。
- **`Any`**（型不明、`extern`経由のみ登場）／**`Error`**／**`Unit`**（値が無いことを表す、サイズ0）／**`Never`**（値が来ないことを表す、`return`直後などで使う特殊型）／**`File`**（不透明なファイルハンドル、フィールド・メソッド無し。生成・操作は13.10節の組み込み関数のみを通す）。
- **`Func`型**：`fn(T1, T2, ...) -> R`という記法で書く関数値の型。引数無しは`fn() -> R`、戻り値無しは`fn(T1, ...) -> Unit`。関数はこの型を持つ第一級の値として変数・引数・戻り値に使える。関数型どうしの`==`比較はできない。

### 2.3 組み込み多相性（「能力」ベース）

`len`・`slice`・`contains`のような組み込み関数の多態は、ユーザーに継承・メソッドとして公開せず、コンパイラ内部限定の非公開「能力（Capability）」タグで実現する。これが`concept.md`要件①「型ごとに別名の関数を乱立させず、単一の直感的な関数名で多様なデータ型を統一的に扱う」の直接の実装方式である。

```
capability Lenable     { String, List[T], Array[T;N], Map[K,V], Set[T], Bytes, Chan[T] }
capability Sliceable   { String, List[T], Array[T;N], Bytes, Stream[T] }
capability Containable { String, List[T], Array[T;N], Map[K,V], Set[T] }
capability Ordered     { Int系, Float系, String }
capability Numeric     { Int系, UInt系, Float系 }
```

静的型付けなので解決は全てコンパイル時に完結する。ユーザー定義capability拡張・同名関数の型ごとの乱立（`str_len`のような別名）は禁止。

---

## 3. リテラルと糖衣構文

### 3.1 リテラル一覧

```amifl
42          1_000_000     0x1F     0o17     0b101
3.14        1.23e4
true        false
"hello\n"
[1, 2, 3]                 // List[Int]（既定。型注釈でArray[T;N]にできる）
{1, 2, 3}                 // Set[Int]
{"a": 1, "b": 2}          // Map[String, Int]
(1, "a", true)            // Tuple3[Int, String, Bool]
Point{x: 1, y: 2}         // struct リテラル
0..10       0..=10        // Range（半開区間／閉区間。内部的にStream[Int]の特殊形）
```

`enum`の値（2.2節）はリテラルではなく`型名.バリアント名(...)`という通常の式で生成する。

### 3.2 添字・スライス糖衣

| 記法 | 展開先 |
|---|---|
| `x[i]` | `at(x, i)` |
| `x[i] = v` | `setAt(x, i, v)` |
| `x[a:b]` | `slice(x, a, b)` |
| `x[a:]` | `slice(x, a, _)` |
| `x[:b]` | `slice(x, _, b)` |
| `x[:]` | `slice(x, _, _)` |

タプルフィールドアクセス`t.0`, `t.1`, ...も糖衣構文。

### 3.3 `?`演算子（エラー短絡）

「戻り値が`Tuple2[T, Error]`である呼び出し式」の直後にのみ書ける後置演算子。エラーがあればゼロ値＋エラーで即`return`する糖衣。**自分を囲む関数の戻り値が`Tuple2[U, Error]`（または`Error`単体）の場合にのみ**使用可。

```amifl
let bytes = readAll(open(path, "r").0)?
```

---

## 4. 定数と変数

```amifl
const Pi: Float = 3.14159265
let count: Int = 0
```

- `let`：実行時変数、初期化式は任意の式、再代入可、**トップレベル宣言は禁止**（グローバル可変状態を避けるため、原則5）。
- `const`：コンパイル時にのみ存在する値、初期化式はリテラルまたは`const`どうしの演算のみ、トップレベル宣言可、再代入不可。参照箇所へインライン展開される。

---

## 5. 式指向設計の原則

- AmiFLに文(statement)という独立した文法カテゴリは無く、プログラムは式の並びだけでできている。
- `{ ... }`ブロックは式であり、値は最後の式の評価結果になる。
- ブロック内の最後以外の式は、その型が`Unit`でなければならない（原則1）。捨てたいときは`_ = 式`と明示する。
- `if`/`switch`/`for`（`yield`付き）/`fn`/`let`/`const`は全て式であり、値を持つ。`return`式も式で、型は`Never`（他のどの型とも合流できる特殊な型）。

```amifl
let grade = if score >= 90 { "A" } elif score >= 70 { "B" } else { "C" }
```

---

## 6. 演算子

- **算術**：`+ - * / %`（`Numeric`に多相。左右の型は完全一致必須）。固定長整数のオーバーフローはラップアラウンド（実行時チェック無し）。ゼロ除算は`Int`系でパニック、`Float`系はInf/NaN。`+`は`Concatenable`（`String`,`Bytes`,`List`,`Array`）に対して結合として働く（`Array`同士の`+`は固定長を維持できないため`List`を返す）。
- **比較**：`== != < <= > >=`。`==`/`!=`は同じ型どうしのみ。`< <= > >=`は`Ordered`のみ。
- **論理**：`&& || !`（`Bool`専用、truthy/falsy変換無し）。
- **ビット**：`& | ^ &^ ~`（整数型専用）。
- **シフト**：`<< >>`（右辺は`UInt`）。
- **パイプ**：`|>`（優先順位は最低＝9。`a + b |> f`は`(a + b) |> f`と読める）。

優先順位（高→低）：`() . [] 関数呼び出し 後置?` → 単項`! - ~` → `* / % << >> & &^` → `+ - | ^` → `< <= > >=` → `== !=` → `&&` → `||` → `|>`

---

## 7. 制御構文（すべて式）

- **`if`/`elif`/`else`**：`else`省略時は`Unit`型限定。`else`ありなら全分岐が同じ型でなければならない。
- **`switch`**：case節に`is Type`（`Any`型限定）・`in [...]`・任意の`Bool`式を書けるパターンマッチ。式として使う場合`default`必須（`enum`の全バリアント網羅時のみ例外で省略可）。
- **`for x in items { ... }`**：`Unit`型、副作用用。**`for x in items yield expr`**：`items |> map(x => expr)`の糖衣構文（専用の実行モデルは無い）。`break`/`continue`は`yield`無し形のみで使用可。
- **`while`**：常に`Unit`型。`break`/`continue`は最も内側のループのみに作用し、クロージャー境界を越えられない。

---

## 8. 関数とクロージャ

### 8.1 基本構文

```amifl
fn add(a: Int, b: Int) -> Int { a + b }
let square = fn(x: Int) -> Int { x * x }
```

- 本体最後の式が戻り値（`return`は早期脱出専用）。
- 多引数がそのまま多引数（**カリー化はしない**。パイプ`|>`＋`_`プレースホルダーで部分適用相当ができるため）。
- **可変長引数は無い**。複数値は`List`/`Tuple`にまとめて渡す。

### 8.2 戻り値は常に単一の型

`-> T`のみ。複数値は`TupleN`で包む。「値+エラー」は必ず`Tuple2[T, Error]`という統一形。この制約は`?`演算子とパイプ演算子が「単一の値が流れる」モデルを前提にしているために必要（原則6）。

### 8.3 関数型

`fn(T1, T2, ...) -> R`。関数値どうしの`==`比較は不可。

### 8.4 引数の渡し方

名前付き（ラベル付き）引数は提供しない。`concept.md`要件③「データを引数として取り、新しいデータを返す素直な関数操作」を満たすには不要な機構であり、名前付き引数を許すと呼び出し順序や省略規則をめぐる新たな文法規則が必要になり、原則4「ユーザー拡張ポイントを絞る」・原則1「特殊構文による乱雑化を避ける」に反する。

引数の意味を呼び出し側から分かりやすくしたい場合は、`struct`にまとめて渡す。

```amifl
struct SortOpt { key: String, desc: Bool }
fn sortBy(xs: List[T], opt: SortOpt) -> List[T] { ... }

rows |> sortBy(_, SortOpt{key: "age", desc: true})
```

デフォルト値付き引数も同様に提供しない。

### 8.5 名前付き`fn`宣言はトップレベルのみ

関数内でローカルに欲しい場合はクロージャー（`let f = fn(...) -> R {...}`）を使う。

```amifl
fn outer() -> Int {
    let inner = fn(x: Int) -> Int { x + 1 }   // OK
    inner(1)
}
```

### 8.6 再帰

トップレベルの名前付き`fn`は自分自身を再帰呼び出しできる。無名クロージャーは自分の名前を持たないため素朴な自己再帰は不可（トップレベルの名前付き`fn`を経由する）。

### 8.7 関数オーバーロードは無い

同名`fn`の複数宣言は型・数が違っても不可（原則4）。

---

## 9. パイプ演算子 `|>`

`_`を明示すればその位置へ、省略時は第1引数へ左辺値を注入する。

```amifl
data |> trim |> upper |> print
data |> slice(_, 0, 5)
rows |> sortBy(_, SortOpt{key: "age", desc: true})
```

右辺がインラインクロージャーの場合、その唯一の引数として左辺値を渡す。

### 9.1 パイプラインDX機能

`tap`・`peek`（13.8節）と、型不整合発生箇所をステージ番号付きで示すコンパイルエラー表示でパイプライン開発体験を支える。

```
error: pipeline type mismatch at stage 3
  data |> parse |> validate |> toUpper |> save
          stage1   stage2      ^^^^^^^  stage4
  stage2 outputs: struct{ok: Bool, value: String}
  stage3 (toUpper) expects: String
  hint: did you mean `toUpper(_.value)` ?
```

### 9.2 組み込み関数の引数順序は「データ第1引数」の慣例で統一する（Step 11で確定）

13節の組み込み関数一覧を見ると、`slice(x, from, to)`・`trim(s)`・`sortBy(xs, opt)`・`push(xs, v)`など、実際にはほぼ全ての関数でデータが第1引数に置かれている。`_`という逃げ道はあるものの、組み込みライブラリ全体の設計慣習としては「データは第1引数」がほぼ前提化しており、`concept.md`「データが常に第1引数へ渡ることは前提としない」という要求の趣旨とは緊張関係にある——という論点をStep 11着手まで未決事項として残していた。

**Step 11で「この慣習を崩さず、一貫して維持する」側に確定した。** 理由：`_`省略時は必ず左辺値が第1引数へ注入される（9節冒頭）ため、「データが第1引数」を組み込みライブラリ全体で一貫させておけば、パイプラインの最も一般的な書き方（`data |> f(otherArgs...)`）が`_`を一度も書かずに済む——`_`の出番を「データが第2引数以降にある例外的な関数」だけに絞れる。一部の関数だけデータを第1引数以外に置く混在設計は、パイプ連鎖のたびに「この関数は`_`が要るか」をユーザーに逐一思い出させることになり、原則7「明示性 > 簡潔さ」よりも認知コストの増大が上回ると判断した。13節の個々の関数シグネチャは、Step 11時点で判断が必要だった全ケースでこの原則に沿って確定させている（CLAUDE.mdのStep 11「確定した設計判断」参照）。

---

## 10. `switch` ＋ 組み込み関数によるパターンマッチ

専用の`match`構文は追加しない（原則3）。`switch`のcase節に`is Type`・`in [...]`・任意の`Bool`式を許可することでパターンマッチを実現する。

```amifl
let label = switch r.status {
    case Status.Ok: "成功"
    case Status.Retry(delay): formatWith("{}秒後に再試行", [delay])
    case Status.Failed(reason): formatWith("失敗: {}", [reason])
}
```

`Status.Retry(delay)`と書くとフィールド`delay`がcase本体スコープで束縛される（別名が欲しければ本体側で`let`し直す）。全バリアントを1回ずつ網羅していれば`default`省略可（コンパイル時にバリアント集合が閉じているため漏れは検出できる）。

---

## 11. 並列処理

専用構文を増やさず、`Chan[T]`／`Stream[T]`という「型」＋既存の`for`＋パイプの組み合わせだけで表現する（`concept.md`要件④に直接対応）。

- `spawn(f: fn() -> Unit)`：goroutine起動（組み込み関数、キーワードではない）。
- `send(ch, v)` / `recv(ch)`：チャネル送受信（`<-`のような専用記号は導入しない、原則3）。`recv`は`Tuple2[T, Bool]`を返す。
- `parallel(s: Stream[T], workers: Int) -> Stream[T]`：Streamに対する組み込み修飾関数。内部でN個のgoroutineへ分配する。

---

## 12. モジュールと複数ファイルの統合

### 12.1 パッケージ＝ディレクトリ

同じディレクトリに置かれた`.aml`ファイル群は、1つの「パッケージ」として1つのプログラムへ統合される。パッケージ**内**のファイル間では`import`は不要——全てのファイルが1つの共有スコープにあるかのようにコンパイルされる（トップレベルの`fn`／`struct`／`enum`／`const`／`capability`宣言を無条件に共有する）。ファイルをどう分割するかは開発者の整理都合であり、言語仕様上の意味は持たない。

```
myproject/
    main.aml
    reading.aml
```

上記2ファイルは`myproject`という1つのパッケージとして一括コンパイルされ、`main.aml`から`reading.aml`で定義した名前をそのまま（`import`なしで）参照できる。

コンパイル対象として**単一ファイル**を直接指定した場合は、そのファイル1つだけの独立したパッケージとして扱う（同じディレクトリに他の`.aml`ファイルがあっても無視する）。

### 12.2 パッケージをまたぐ参照

別のディレクトリにあるパッケージを参照したい場合は`import <エイリアス> "<相対パス>"`を使う（パスは文字列リテラルのみ、コンパイル時解決）。パスは参照元ファイル自身のディレクトリからの相対パス。`import`文はトップレベルであればファイル中どこに書いてもよい。

```amifl
import mathutil "./mathutil"

fn main() -> Int {
    print(format(mathutil.clamp(15, 0, 10)))
    0
}
```

他パッケージから参照できるのは、**名前が大文字で始まるトップレベル宣言だけ**（Goの可視性規則を採用。実行基盤がGoである以上、生成物がそのまま正規のGoパッケージ群になるため、Goのツールチェーンとの親和性を優先した）。値の種類（`fn`／`struct`／`enum`／`const`）は問わない。

```amifl
// mathutil/clamp.aml
fn Clamp(v: Int, lo: Int, hi: Int) -> Int {
    if v < lo { lo } elif v > hi { hi } else { v }
}

fn helper(x: Int) -> Int { x }   // 小文字始まりなので他パッケージから見えない
```

**`エイリアス.名前`という参照は、3.2節のタプル／構造体フィールドアクセスの`.`とは別の構文的位置づけであり、コンパイル時にのみ意味を持つ**（15節の`extern`名前空間参照と同じ立ち位置）。`mathutil.clamp(15, 0, 10)`は`clamp(15, 0, 10)`という素直な呼び出しへコンパイル時に直接解決される。

### 12.3 `main`はルートパッケージでのみエントリーポイントになる

`main`（14節）は、コンパイル時にルートとして指定したパッケージ（通常はプロジェクト最上位のディレクトリ、または直接指定した単一ファイル）に書いたものだけが実際のエントリーポイントとして扱われる。`import`で取り込まれている側（非ルートパッケージ）が偶然`main`という名前をトップレベルで使っていても、コンパイルエラーにはしない——12.4節の機械的なリネームがそのまま適用され、他の名前と同じように内部的な私的束縛になるだけで、エントリーポイントとしては一切扱われない（公開されていれば、取り込む側から`エイリアス.main`のように参照することもできる、ただの1関数として）。

### 12.4 コンパイル時の名前解決

複数ファイル・複数パッケージは、実行が始まる前に**1つのプログラムへ静的に統合**される——修飾子付き参照（`mathutil.clamp`）は対象パッケージの宣言へコンパイル時に直接解決され、実行時に名前で検索する仕組みは一切経由しない（AmiFLは静的型付けであり、0節の前提の通り実行時の動的ディスパッチが無いため）。

この統合の過程で、非ルートパッケージのトップレベル宣言（公開・非公開を問わない——非公開の名前も、同じパッケージ内の公開された宣言から参照される可能性があるため、プログラムには組み込まれる）は全て、一意な名前へ機械的に置き換えられる。ユーザーがこの置き換え後の名前を直接書く必要はなく、修飾子付き参照は常にコンパイラが自動的に解決する。**ルートパッケージ自身の宣言には、この名前の書き換えを行わない**（ルートパッケージが1つに定まるため、一意化の必要が無い）。

**この機械的な名前の置き換えは、パッケージのディレクトリ名ではなく、`import`文で与えたエイリアスそのものを使う。** `import mathutil "./mathutil"`なら内部的に`mathutil_Clamp`のような名前になる——ディレクトリ名は取り込み元のファイルの外からは見えない実装詳細に過ぎない。この設計により、別々の場所にある2つのパッケージが偶然同じディレクトリ名を持っていても（例:`foo/utils`と`bar/utils`）、それぞれ別のエイリアスで取り込む限り（`import utilsA "./foo/utils"` / `import utilsB "./bar/utils"`）衝突しない。

その代わり、**同じエイリアスを2つの異なるパッケージへ割り当てようとすると、コンパイルエラーになる。** 1つのファイル内で同じエイリアスを別のパッケージへ再`import`した場合も、別々のパッケージがそれぞれ独立に同じエイリアス名を選んでしまった場合も、どちらも同じ理由（一意化後の名前が衝突する）で拒否される。開発者は、取り込み先ごとに異なるエイリアスを選ぶことで回避する。

### 12.5 パッケージ間の依存

パッケージ間の依存は有向非巡回グラフでなければならない（循環禁止）。パッケージAがパッケージBを`import`し、BもAを直接・間接に`import`している場合はコンパイルエラーとする。

---

## 13. 組み込み関数 完全一覧

可変長引数は一切持たない。組み込み関数はいずれも**インポート不要でグローバルスコープに存在**する（`concept.md`「参考概念：組み込み関数の領域設計」のC言語アナロジーに対応）。

| 領域 | 対応するCヘッダのイメージ | 該当区分 |
| :--- | :--- | :--- |
| I/O・ストリーム領域 | `stdio.h` | 13.1（出力・終了）、13.8（チャネル・ストリーム・並列）、13.10（ファイルI/O） |
| データ操作領域 | `string.h`拡張 | 13.4（データ操作）、13.5（集合）、13.6（Map） |
| 型・基礎操作領域 | `stdlib.h` | 13.2（型・値判定）、13.3（変換）、13.7（数値） |

### 13.1 出力・終了

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `print` | `(v: Any) -> Unit` | 標準出力へ書き、末尾に改行 |
| `eprint` | `(v: Any) -> Unit` | 標準エラー出力版`print` |
| `format` | `(v: Any) -> String` | 値を文字列表現へ変換する |
| `formatWith` | `(template: String, args: List[Any]) -> String` | `{}`をプレースホルダーとする簡易テンプレート展開（可変長引数を避けるため引数はList化） |
| `exit` | `(code: Int) -> Never` | プログラムを終了コードで終了する |

### 13.2 型・値判定

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `typeName` | `(v: Any) -> String` | `extern`由来の`Any`値の動的型名を返す |
| `isError` | `(v: Any) -> Bool` | 値が`Error`（非nil）かどうか |

### 13.3 変換

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `cast[T]` | `(v: Numeric) -> T` | 数値型どうしの明示変換 |
| `parse[T]` | `(s: String) -> Tuple2[T, Error]` | 文字列→数値/真偽値へのパース |

### 13.4 データ操作（多相組み込み関数、2.3節参照）

| 関数 | 対象型 |
|---|---|
| `len(x) -> Int` | String, List, Array, Map, Set, Bytes, Chan |
| `slice(x, from, to)` | String, List, Array, Bytes, Stream |
| `at(x, i)` / `setAt(x, i, v)` | List, Array（`setAt`はListのみ。固定長は`x[i]=v`のみ） |
| `contains(x, target) -> Bool` | String, List, Array, Map(キー), Set |
| `index(x, target) -> Tuple2[Int, Bool]` | String, List, Array |
| `split(s, sep)` / `join(xs, sep)` | String |
| `replace(s, old, new)` / `trim(s)` / `upper(s)` / `lower(s)` | String |
| `startsWith(s, prefix)` / `endsWith(s, suffix)` | String |
| `map(xs, f)` / `filter(xs, f)` | List, Array, Stream, Chan |
| `reduce(xs, init, f)` | List, Array, Stream |
| `sort(xs)` / `sortBy(xs, opt)` | List |
| `reverse(xs)` | List, Array, String |
| `unique(xs)` / `flatten(xs)` / `zip(xs, ys)` | List（`flatten`は`List[List[T]]`） |
| `push(xs, v)` / `pop(xs)` / `insert(xs, i, v)` / `removeAt(xs, i)` | List |
| `concat(a, b)` | List, String, Bytes |

### 13.5 集合(`Set[T]`)専用

| 関数 | 説明 |
|---|---|
| `add(s, v)` / `discard(s, v)` | 要素の追加／削除 |
| `union(a, b)` / `intersect(a, b)` / `difference(a, b)` | 集合演算 |
| `toList(s)` | `List`化（順序は不定） |

### 13.6 `Map[K,V]`専用

| 関数 | 説明 |
|---|---|
| `keys(m)` / `values(m)` / `entries(m)` | |
| `get(m, k, default)` | 無ければ`default` |
| `set(m, k, v)` / `delete(m, k)` | |

### 13.7 数値

| 関数 | 説明 |
|---|---|
| `min(a, b)` / `max(a, b)` / `abs(v)` / `clamp(v, lo, hi)` | |
| `round(v)` / `floor(v)` / `ceil(v)` | |
| `pow(base, exp)` / `sqrt(v)` | |

### 13.8 チャネル・ストリーム・並列

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `chan[T]` | `(buffer: Int) -> Chan[T]` | |
| `send` / `recv` | `(ch, v) -> Unit` / `(ch) -> Tuple2[T, Bool]` | |
| `spawn` | `(f: fn() -> Unit) -> Unit` | goroutine起動 |
| `parallel` | `(s: Stream[T], workers: Int) -> Stream[T]` | |
| `collect` | `(s: Stream[T]) -> List[T]` | |
| `take` / `skip` | `(s: Stream[T], n: Int) -> Stream[T]` | |
| `tap` | `(v: T, label: String) -> T` | 恒等関数＋ログ出力 |
| `peek` | `(v: T) -> T` | 開発モード限定の対話的インスペクタ |

### 13.9 エラー処理

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `unwrap[T]` | `(t: Tuple2[T, Error]) -> T` | エラーがあればその場でパニック（プロトタイピング用途） |
| `okOr[T]` | `(t: Tuple2[T, Error], default: T) -> T` | エラーなら`default`を返す |

### 13.10 ファイルI/O

| 関数 | シグネチャ | 説明 |
|---|---|---|
| `open` | `(path: String, mode: String) -> Tuple2[File, Error]` | `mode`は`"r"` `"w"` `"a"` |
| `close` | `(f: File) -> Error` | |
| `read` / `readAll` / `readLine` | `(f: File, ...) -> Tuple2[..., Error]` | |
| `lines` | `(f: File) -> Stream[String]` | |
| `write` | `(f: File, data: Bytes) -> Tuple2[Int, Error]` | |
| `stdin` / `stdout` / `stderr` | `() -> File` | |

---

## 14. エントリーポイント

```amifl
fn main(args: List[String]) -> Int {
    print("Hello, AmiFL!")
    0
}
```

`main`はルートパッケージにのみ書ける予約名。引数に興味が無い場合`fn main() -> Int { ... }`と書ける。

---

## 15. `extern`機構（Go資産の利用）

### 15.1 関数のバインド

```amifl
extern "encoding/json" as json {
    bind Marshal(v: Any) -> Tuple2[Bytes, Error]
    bind Unmarshal(data: Bytes, out: Any) -> Error
}
```

`extern "パス" as 名前空間 { bind ... }`はGoパッケージをホワイトリスト方式で取り込む。`bind`されたGo関数は、`名前空間.関数名`のような修飾なしに、AmiFLのグローバルスコープへ直接束縛される裸の名前として呼び出す（`.`を使った呼び出し構文は存在しない——後述15.2節）。`名前空間`（`as`の右辺）はAmiFLソース内では参照されず、コンパイラが生成コードに挿入するGo importのエイリアスとしてのみ使われる。

`bind`のGo側の実際の呼び出し対象は、既定では`Name`（bind名）と完全に同じ名前のパッケージレベル関数だが、`as GoTarget`で明示的に指定できる（Step 13で確定。関数のオーバーロードを禁止するAmiFLでは、AmiFL側の名前とGoの実際の関数名が食い違う場面——同名メソッドを複数の型がそれぞれ持つ場合等（15.2節参照）——が起こりうるため）。`GoTarget`は次の2形式のいずれか：

- 裸の識別子（例: `as Marshal`）：`Name`とは異なる名前のパッケージレベル関数を指す
- `Type.Method`（例: `as Time.Unix`）：15.2節のメソッドバインド専用。ドットの有無で2形式を区別する

### 15.2 Goのメソッドの扱い

`method`宣言のようなGoのレシーバ構文を持つ専用構文は設けない。Goのレシーバ付きメソッドは、レシーバを普通の第1引数として受け取る関数として`bind`する（`concept.md`要件⑤「メソッドやレシーバ概念を剥ぎ取った上で、データ型・純粋関数へ変換」）。

```amifl
extern "time" as time {
    type Time
    bind Now() -> Time
    bind TimeUnix(t: Time) -> Int as Time.Unix
    bind TimeFormat(t: Time, layout: String) -> String as Time.Format
}

fn todayString() -> String {
    Now() |> TimeFormat(_, "2006-01-02")
}
```

AmiFLに`.`を使ったメソッド呼び出し構文は存在しない（`.`はフィールド／タプルアクセス専用、3.2節・3.3節）。よってGo側のメソッド名がそのまま使えるとは限らず、型名を接頭辞にするなどして関数のオーバーロード禁止（原則4）と衝突しないよう命名する必要がある。この命名は`extern`ブロックを書く側の責務とし、コンパイラによる自動解決は行わない——実際にどのGoメソッドを呼ぶかは、`bind`の`as Type.Method`節（第1引数がレシーバ、`Type`はその宣言型と一致必須）で明示する。

### 15.3 複数文にまたがるGoロジックが必要な場合

単一の関数・メソッド呼び出しに収まらない複雑なGoロジックが必要な場合も、専用構文は用意しない。同じディレクトリに手書きの`.go`ファイルを置き、そこで定義した関数を`extern`で束ねる、という15.1節と同じ経路に統一する。Go資産へのアクセスは常に「関数という名前の付いた単位を`bind`する」という一貫した経路のみで行い、AmiFLソース内にGo構文を逐語的に埋め込む特殊経路は存在しない。

### 15.4 コンパイラ組み込み型の扱い

`File`・`Chan[T]`のようなコンパイラ組み込み型はユーザーが`extern`で独自に上書きすることはできない（`bind`できるのは名前空間内のGo関数のみ）。

---

## 16. プロジェクト構成とツールチェイン

weaveの実ディレクトリ構成・CLIコマンド体系をAmiFLの標準として踏襲する（対象がAmiFLソース〈`.aml`ファイル〉であることが分かるよう名称のみ読み替える）。

### 16.1 ディレクトリ構成

```
amifl/                          AmiFLのホーム（このリポジトリのルート）
  amifl-spec.md                 AmiFL言語仕様（唯一の正）
  amifl_implementation_notes.md AMIVM-IR生成に関する実装知見（次にAMIVM上で言語を実装する人向け）
  CLAUDE.md                     プロジェクト規約
  LICENSE                       MIT
  README.md / README_ja.md      導入ドキュメント（英語版/日本語版）
  go.mod                        module github.com/amisonnet8/amifl
  Makefile                      build/install/test/test-examples/fmt/vet/tidy/clean タスク
  .github/workflows/test.yml    CI: push/PR時にgofmt/go vet/go test/make test-examplesを実行
  cmd/amifl/
    main.go                     CLIエントリポイント（build/run/emit-ir/emit-go/archive/help のディスパッチ）
    build.go                    modloader.Load → sema.Check → codegen.Generate → amivm(-IR生成) →
                                 go build のパイプライン
    archive.go                  `amifl archive` サブコマンド（12節のパッケージを配布アーカイブへまとめる）
  internal/lexer/                字句解析
  internal/parser/               構文解析 → AST
  internal/ast/                  AST定義
  internal/modloader/            パッケージ取り込みの解決（12節）。複数`.aml`ファイル・複数パッケージ
                                  （ディレクトリまたは`.amlz`アーカイブ）を1つのフラットな内部表現へ解決してから
                                  sema/codegenへ渡す
  internal/sema/                 意味検査（型チェック・スコープ解決・capability解決・パイプライン型接続検査等）
  internal/codegen/              AST → AMIVM-IR生成
  amiflrt/                       AmiFL独自ランタイム（go:embedで配布。`Stream[T]`/`Chan[T]`/`File`等の
                                  組み込み型・13節の組み込み関数のうちAMIVM命令に対応しないものの実装）
  examples/                      サンプルAmiFLプログラム（`.aml`。実装した構文ごとに追加）
  tour/                          AmiFL言語入門（章ごとに1ファイルのマークダウン）
```

### 16.2 コマンドライン・インターフェース

```
amifl <command> [flags] <file.aml | package-dir | package.amlz>
```

| コマンド | 説明 |
|---|---|
| `build` | ネイティブ実行ファイルへコンパイルする |
| `run` | コンパイルして即座に実行する（stdin/stdout/stderrをストリームする） |
| `emit-ir` | AMIVM-IRへコンパイルする |
| `emit-go` | Goソースへコンパイルする（amivm経由） |
| `archive` | ディレクトリ直下の`.aml`ファイル群を`.amlz`アーカイブ（12節のパッケージの配布形式）へまとめる |
| `help` | ヘルプメッセージを表示する |

`build`／`emit-ir`／`emit-go`共通フラグ：

| フラグ | 説明 |
|---|---|
| `-o <file>` | 出力ファイルパス（省略時は入力パスから導出） |
| `-v` | 各パイプライン段階（AMIVM-IR・型チェック過程・最終生成コード）の出力を表示する |

`archive`のフラグ：

| フラグ | 説明 |
|---|---|
| `-o <file>` | 出力`.amlz`パス（省略時は`<ディレクトリ名>.amlz`） |

`package-dir`（またはその`.amlz`アーカイブ）を指定した場合、そのパッケージ配下の全`.aml`ファイルを1つのプログラムとしてコンパイルする（12.1節）。単一ファイルを指定した場合は、そのファイル1つだけをコンパイルする（同じディレクトリの他ファイルは無視する）。

---

## 17. 未決事項・検討事項

### 17.1 コンセプト適合上の検討事項

✅ **組み込み関数の引数順序慣例**：Step 11で解決済み（9.2節参照）。「データ第1引数」の慣例を崩さず一貫させる方針に確定した。

### 17.2 一般的な技術的未決事項（コンセプトとの矛盾ではなく、実装完成度の問題）

| # | 事項 | 方向性の提案 |
|---|---|---|
| 1 | `?`演算子の3値以上への一般化 | 一般化しない。「戻り値は常に単一の型」という原則6を貫くなら`Tuple2[T,Error]`限定を維持する方が一貫性が高い。 |
| 2 | `Set[T]`の要素順序（`toList`の結果順）が不定 | `OrderedSet`は増やさない（原則3）。`toList(_) |> sort`という組み合わせ運用に統一する方向を提案。 |
| 3 | ビルドキャッシュ・増分コンパイルの仕組み | 本書（言語仕様）の対象外。 |
| 4 ✅ | `Stream[T]`と`Chan[T]`の間の暗黙変換（自動昇格） | Step 12で確定：許可しない（原則2「暗黙変換の排除」との整合）。明示変換関数（`toStream(ch)`等）は13節の組み込み関数一覧に含まれていないため未実装のまま——具体的な必要が出た時点で追加を検討する。 |
| 5 | `extern`のバージョニング | Go側APIが変わった場合の`bind`宣言との不整合検知の仕組みは未検討。 |
